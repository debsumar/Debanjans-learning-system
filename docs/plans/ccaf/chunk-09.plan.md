## Chunk metadata
- UNVERIFIED: Topic: Claude Certified Architect - Foundations (ccaf), exam code CCA-F.
- Source: `/tmp/ccaf-chunks/chunk8`; transcript global lines 14729-16569; 1841 source lines read in four 500-line slices through EOF.
- Scope: multi-instance review, validation remediation, large-context controls, durable codebase exploration, and observed orchestration failure modes.
- Evidence rule: lesson facts below derive only from cited transcript spans. Code/demo names are source examples, not required implementation choices.
- Planning boundary: no HTML, CSS, JS, registry edits, or topic directory creation.

## Lesson inventory
| # | title | chunk line refs | one-line summary |
|---|---|---|---|
| 1 | Independent multi-instance review | 15304-15488 | Separate generation and review contexts to reduce self-review anchoring. |
| 2 | Validation, retry, and remediation loops | 15489-15670 | Return specific validation errors to agent loop, retry within a cap, and stop when appropriate. |
| 3 | Large-context failure modes | 15671-15747 | Protect precise facts, mitigate lost-in-the-middle, and avoid passing unneeded tool output. |
| 4 | Persisted facts and filtered tool output | 15748-15929 | Externalize case facts and reduce bulky state to prompt-relevant fields. |
| 5 | Context-safe codebase exploration architecture | 15930-16161 | Keep coordinator context clean through subagents, findings artifacts, and manifest-backed recovery. |
| 6 | Explorer and synthesizer contracts | 16162-16468 | Define constrained explorer and synthesis roles, scratchpads, manifest updates, and append-only findings. |
| 7 | Orchestration evaluation and recovery gaps | 16469-16569 | Test failure handling, reconcile stuck work, explicitly invoke coordination, and avoid conflicting control designs. |
| 8 | Provenance, observability, and rate-limit diagnosis | 14729-15303 | Detect missing source/tool use, expose progress, limit concurrency, and distinguish incomplete artifacts from success. |

## Per-lesson plan
### 1. Independent multi-instance review
- Key concepts: self-review anchoring; multi-turn history bias; separate peer reviewer; fresh context; generator/reviewer role separation; quality control for generated content.
- UNVERIFIED: Exam-relevant facts: Source says a model reviewing work in same conversation retains generation history and may believe its own work is good; a separate agent, model, or empty/new context avoids that prior context; recommendation: generator must not grade its own output in same conversation (15304-15488).
- Candidate objectives: `ccaf-cNN-o1` (NN TBD): Explain why an independent review context can reduce bias when assessing generated output. `ccaf-cNN-o2` (NN TBD): Select a generator/reviewer separation for AI-generated content requiring quality control.
- Glossary terms with g- slugs: g-self-review-anchoring; g-multi-turn-context; g-independent-reviewer; g-peer-review; g-fresh-context; g-quality-control.
- Diagram candidates with archetype from concept|process|comparison|decision|worked-scenario|callout|diagram|mcq-set|active-recall: comparison: same-conversation self-review versus fresh-context peer review, showing retained history only on self-review path; process: generate output -> pass artifact to independent reviewer -> review findings.
- Comparison/decision table candidates with rows and columns: Rows: same conversation self-review, separate agent review, new empty-context review. Columns: prior generation history available, self-anchoring risk, stated use for quality control, review independence.
- UNVERIFIED: MCQ candidates (count + applied stem ideas): 3. A vocabulary-question generator says all items are correct after reviewing in its original chat; choose best review architecture. A team needs quality control for generated document; choose context relationship. Identify claim unsupported by source: independent review guarantees correctness.
- UNVERIFIED: Recall prompts (count + prompts): 3. Why can same-conversation self-review be biased? What reviewer context does source recommend for quality-controlled generated content? What does independent review not prove by itself?
- Confusion pairs with discriminator: independent reviewer versus self-reviewer: independent reviewer lacks generator conversation history; separate agent versus same model: source permits another model, another agent, or empty/new context, so discriminator is context independence rather than model identity.
- Common wrong turn misconceptions: A second prompt in same conversation is peer review. Fresh review guarantees generated content is correct. JSON parse success proves semantic quality.
- Causal step chains only if genuine, else 'none - omission beats invented process': 1. Generator produces artifact. 2. Same conversation retains prior reasoning and choices. 3. Self-review can be anchored to that work. 4. Separate/new context reviews without that history.

### 2. Validation, retry, and remediation loops
- Key concepts: structured JSON; schema/syntax versus semantic validation; explicit error feedback; tool-result error state; retry loop; maximum attempts; remediation; absent source information.
- UNVERIFIED: Exam-relevant facts: Source demonstrates validation errors returned into an agent loop, with specific errors appended to the prompt; agent receives errors, fixes output, and retries; loop has max attempts; retry may be inappropriate when required source information is absent; source distinguishes structural/schema concerns from data-quality or semantic validation concerns (15489-15670).
- Candidate objectives: `ccaf-cNN-o3` (NN TBD): Design a validation-retry loop that feeds precise failures back to an agent. `ccaf-cNN-o4` (NN TBD): Distinguish schema/syntax validation from semantic or business-rule validation in an agent workflow. `ccaf-cNN-o5` (NN TBD): Identify when bounded retries should stop rather than continue.
- Glossary terms with g- slugs: g-validation-loop; g-remediation; g-retry-cap; g-specific-validation-error; g-tool-result-error; g-schema-validation; g-semantic-validation; g-business-rule.
- Diagram candidates with archetype from concept|process|comparison|decision|worked-scenario|callout|diagram|mcq-set|active-recall: process: agent response -> validator -> pass/end or error feedback -> corrected retry -> maximum-attempt stop; comparison: schema/syntax error versus semantic/business-rule error; decision: retry when correction may satisfy stated validation versus stop/escalate when required source fact is absent.
- Comparison/decision table candidates with rows and columns: Rows: valid output, schema/syntax failure, semantic/business-rule failure, required information absent, retry cap reached. Columns: validator result, feedback supplied, retry appropriate, terminal action.
- UNVERIFIED: MCQ candidates (count + applied stem ideas): 4. Triage output has tags not in snake case; choose feedback-loop behavior. JSON is valid but ticket summary violates business rule; classify validation. Input document lacks required value; choose retry policy. Agent has exhausted max attempts; choose next action.
- UNVERIFIED: Recall prompts (count + prompts): 4. What must validation feedback contain to support remediation? Why cap retries? Contrast schema/syntax and semantic validation. Name condition source identifies as potentially not worth retrying.
- Confusion pairs with discriminator: validation retry versus blind regeneration: retry carries specific failure details; schema/syntax error versus semantic validation error: first concerns required structure, second correctness/quality of values or business rules.
- Common wrong turn misconceptions: Validation means JSON parsing only. Retrying without sending error details is remediation. More retries can create missing source information. A retry loop needs no terminal cap.
- Causal step chains only if genuine, else 'none - omission beats invented process': 1. Agent produces tool/output data. 2. Validator runs stated checks. 3. Validator returns precise failures as error result. 4. Loop appends those failures to subsequent context. 5. Agent corrects and resubmits. 6. Stop on pass or maximum attempts; consider terminating earlier if source lacks required data.

### 3. Large-context failure modes
- Key concepts: progressive summarization; precision loss; critical dates, percentages, and numbers; lost-in-the-middle effect; section processing; position of important information; external API JSON bulk; conversation summarization under memory limits.
- UNVERIFIED: Exam-relevant facts: Source says repeated summarization can make dates, percentages, numbers, and key information generic or lost; preserve/extract precise information and inject it into later context; models may attend to beginning/end of large input while missing middle; process data in sections; external tools may return JSON much larger than needed, so do not pass full response when only key fields are needed; long conversations are summarized as memory/context limits are reached (15671-15747).
- Candidate objectives: `ccaf-cNN-o6` (NN TBD): Identify progressive-summarization risk and preserve high-precision facts across iterations. `ccaf-cNN-o7` (NN TBD): Mitigate lost-in-the-middle risk in a large source. `ccaf-cNN-o8` (NN TBD): Reduce context load from external tool/API responses without requiring an LLM for trivial filtering.
- Glossary terms with g- slugs: g-progressive-summarization; g-precision-fact; g-lost-in-the-middle; g-context-window; g-section-processing; g-tool-output-filtering; g-api-response-bulk; g-conversation-summarization.
- Diagram candidates with archetype from concept|process|comparison|decision|worked-scenario|callout|diagram|mcq-set|active-recall: concept: large input with beginning/end attention and vulnerable middle; process: extract precision facts -> external store -> inject into next prompt; comparison: full API response versus selected required fields; callout: dates, percentages, numbers, and key information are preservation candidates.
- UNVERIFIED: Comparison/decision table candidates with rows and columns: Rows: progressive summary, section-by-section processing, important facts stored/injected, full API JSON, filtered API fields. Columns: context volume, precision retention, middle-content risk, source-stated mitigation.
- UNVERIFIED: MCQ candidates (count + applied stem ideas): 4. Iterative summary loses a regulatory percentage; choose mitigation. Important evidence is buried midpoint in 50-page input; choose handling. Flight-search tool returns extensive JSON but task needs order number; choose payload. Long agent conversation approaches context limit; identify behind-the-scenes pressure described.
- UNVERIFIED: Recall prompts (count + prompts): 4. What facts does source name as vulnerable during repeated summarization? Define lost-in-the-middle in source terms. What is preferred broad mitigation for large document input? Why should full external JSON normally not be returned to model?
- Confusion pairs with discriminator: progressive-summary loss versus lost-in-the-middle: first loses precision across condensation steps; second misses information located in middle of a large prompt; filtering tool output versus asking an LLM to filter: ordinary selection can occur without an LLM.
- Common wrong turn misconceptions: A larger context removes all attention problems. Summaries preserve automatically important numbers. Put critical material only at one end as universal solution. Every tool result should be passed back unchanged.
- Causal step chains only if genuine, else 'none - omission beats invented process': 1. Repeated condensation removes or generalizes precision. 2. Extract high-value facts before condensation. 3. Store them externally. 4. Reinject exact facts into later context. Separate chain: 1. Tool returns bulky response. 2. Select required fields. 3. Pass reduced result to model.

### 4. Persisted facts and filtered tool output
- Key concepts: external memory; case facts; persistent file storage; load/save facts; world state; relevance filtering; text-adventure demonstration; current room; iterative prompt context.
- UNVERIFIED: Exam-relevant facts: Source demo stores selected case facts in a file, loads them into context on each loop, and filters a large simulated world-state/tool response down to relevant information; source frames this as solving key-information extraction and tool-output reduction, while not demonstrating section processing for a very large document (15748-15929).
- Candidate objectives: `ccaf-cNN-o9` (NN TBD): Plan persistent fact storage and reinjection for iterative agent interactions. `ccaf-cNN-o10` (NN TBD): Specify prompt-relevant filtering of a large tool response. `ccaf-cNN-o11` (NN TBD): Separate demonstrated large-context controls from controls not demonstrated in a given example.
- Glossary terms with g- slugs: g-external-memory; g-case-facts; g-persistent-facts-file; g-world-state; g-relevant-field-filter; g-iterative-prompt; g-current-state; g-context-reinjection.
- Diagram candidates with archetype from concept|process|comparison|decision|worked-scenario|callout|diagram|mcq-set|active-recall: worked-scenario: text adventure gets full world state, selects current-room facts, loads case-facts file, then constructs next prompt; process: tool response -> field filter -> reduced context plus persisted facts -> agent turn; comparison: in-memory-only facts versus persisted/reloaded facts.
- Comparison/decision table candidates with rows and columns: Rows: full world state, selected current-state fields, persisted case facts, unsaved in-memory facts. Columns: retained across run/reload, prompt token burden, relation to current turn, source demonstration status.
- UNVERIFIED: MCQ candidates (count + applied stem ideas): 3. Adventure agent receives huge state but must answer current-room action; select context payload. Restarted iterative workflow needs previously extracted fact; select storage/reload behavior. Identify mitigation source says demo did not test.
- UNVERIFIED: Recall prompts (count + prompts): 3. What are case facts used for in the demo? What happens to world-state data before it enters prompt? Which large-context mitigation remains undemonstrated in this example?
- Confusion pairs with discriminator: case facts versus complete world state: case facts are selected, persistent prompt-relevant information; filtering versus summarization: source selects needed fields from tool output rather than asking model to restate all content.
- Common wrong turn misconceptions: External memory means always inject every stored fact. A file-backed fact artifact is equivalent to entire repository/context. Demo proves all large-document chunking strategies.
- Causal step chains only if genuine, else 'none - omission beats invented process': 1. Tool yields large world state. 2. System selects data needed for current context. 3. System loads persisted case facts. 4. System builds prompt from reduced current state plus facts. 5. Later turns save/reload facts as needed.

### 5. Context-safe codebase exploration architecture
- Key concepts: large codebase exploration; coordinator; subagent isolation; external findings; manifest state; crash recovery; task resumption; parent context cleanliness; source-read restrictions.
- UNVERIFIED: Exam-relevant facts: Source proposes external findings and manifest artifacts because a main coordinator ingesting busy codebase details can become muddled; subagent handles noisy raw exploration while coordinator uses findings; manifest stores task state for recovery after shutdown/error/credit exhaustion; source proposes parent/coordinator should not directly read raw code while explorer does; Doom source is an illustrative codebase only (15930-16161).
- Candidate objectives: `ccaf-cNN-o12` (NN TBD): Design a coordinator/subagent exploration split that protects coordinator context. `ccaf-cNN-o13` (NN TBD): Use findings and manifest artifacts for durable progress and recovery. `ccaf-cNN-o14` (NN TBD): State least-access boundaries between coordination and raw code exploration roles.
- Glossary terms with g- slugs: g-codebase-exploration; g-coordinator; g-subagent-isolation; g-findings-artifact; g-task-manifest; g-crash-recovery; g-resume-state; g-parent-context; g-raw-source-boundary.
- UNVERIFIED: Diagram candidates with archetype from concept|process|comparison|decision|worked-scenario|callout|diagram|mcq-set|active-recall: architecture diagram: coordinator -> explorer subagent -> findings and manifest; synthesizer reads findings; coordinator does not read raw source; process: task dispatch -> explore -> persist findings/state -> recover/resume after interruption.
- Comparison/decision table candidates with rows and columns: Rows: coordinator directly explores source, isolated explorer explores source, coordinator reads findings, synthesizer reads scratchpads. Columns: raw-source access, coordinator-context load, durable artifact, recovery role.
- UNVERIFIED: MCQ candidates (count + applied stem ideas): 4. Large repository investigation makes coordinator lose thread; choose architecture. Machine stops mid-task; identify persisted artifacts enabling resume. Select actor that should ingest raw code in proposed model. Identify non-goal of findings artifact.
- UNVERIFIED: Recall prompts (count + prompts): 4. Why send raw exploration to a subagent? What role does findings artifact serve? What state belongs in manifest? Why is raw-source access constrained for coordinator?
- Confusion pairs with discriminator: findings artifact versus manifest: findings hold exploration knowledge; manifest holds task/state tracking; coordinator versus explorer: coordinator dispatches/uses artifacts, explorer reads raw source.
- Common wrong turn misconceptions: More raw repository content makes coordinator better at coordination. A completed chat transcript is adequate crash recovery. Coordinator must directly inspect raw source to direct work. Findings and manifest are interchangeable.
- Causal step chains only if genuine, else 'none - omission beats invented process': 1. Coordinator receives question/task. 2. Explorer subagent reads raw code and investigates. 3. Explorer persists results to findings and state to manifest. 4. Coordinator references artifacts rather than raw source. 5. After interruption, state enables task recovery/resume.

### 6. Explorer and synthesizer contracts
- Key concepts: explorer contract; synthesizer contract; task ID; question; scratchpad; append-only writes; key files; entry points; call chain; synthesis; source provenance; agent definitions.
- UNVERIFIED: Exam-relevant facts: Source walkthrough describes explorer constrained to findings paths and manifest, forbidden from modifying source; explorer derives slug/task data, writes scratchpad fields including question, entry points, call chain, key files, and summary, appends rather than rewrites; separate synthesizer consolidates pre-task scratchpads into coherent document; source later observes tension between agent-file definitions and hardcoded/dynamic definitions (16162-16468).
- Candidate objectives: `ccaf-cNN-o15` (NN TBD): Define an exploration-agent contract with constrained write scope and required findings fields. `ccaf-cNN-o16` (NN TBD): Define a synthesizer that combines scratchpads without raw-source exploration. `ccaf-cNN-o17` (NN TBD): Identify a contract/design ambiguity when multiple agent-definition mechanisms overlap.
- Glossary terms with g- slugs: g-agent-contract; g-write-scope; g-scratchpad; g-append-only; g-task-id; g-entry-point; g-call-chain; g-key-files; g-synthesizer; g-agent-definition.
- Diagram candidates with archetype from concept|process|comparison|decision|worked-scenario|callout|diagram|mcq-set|active-recall: process: coordinator passes task ID/question -> explorer writes append-only scratchpad and manifest -> synthesizer reads scratchpads -> consolidated synthesis; comparison: explorer versus synthesizer permissions and inputs; callout: append-only preserves prior scratchpad sections.
- Comparison/decision table candidates with rows and columns: Rows: explorer, synthesizer, coordinator. Columns: reads raw source, writes scratchpad, writes manifest, reads scratchpads, modifies source, primary output.
- UNVERIFIED: MCQ candidates (count + applied stem ideas): 4. Choose allowable explorer operation under source contract. Select fields required in investigation scratchpad. A component must combine several research outputs; choose role. Team has file-based agent definitions plus overriding definitions; identify architectural ambiguity to resolve.
- UNVERIFIED: Recall prompts (count + prompts): 4. What source files should explorer not modify? Name four described scratchpad contents. Why append rather than rewrite prior scratchpad sections? What is synthesizer input/output role?
- Confusion pairs with discriminator: explorer versus synthesizer: explorer performs raw investigation and creates per-task artifacts; synthesizer merges existing artifacts; append-only versus immutable: append-only permits new sections but forbids rewriting earlier sections.
- Common wrong turn misconceptions: Synthesizer needs raw-source access to integrate findings. Explorer should update source while investigating. A scratchpad needs only final answer and no evidence-routing fields. Multiple definition sources automatically remain consistent.
- Causal step chains only if genuine, else 'none - omission beats invented process': 1. Coordinator supplies task question and ID. 2. Explorer investigates permitted source. 3. Explorer appends structured scratchpad and updates task state. 4. Synthesizer reads multiple scratchpads. 5. Synthesizer produces consolidated answer artifact.

### 7. Orchestration evaluation and recovery gaps
- Key concepts: smoke test; happy path; parent-side crash detection; stuck running state; reconciliation; failed status; resume; coordinator invocation; instructions versus skill; phase overlap; results JSON versus scratchpad synthesis.
- UNVERIFIED: Exam-relevant facts: Source identifies implemented flow as happy path only before adding failure handling; proposed recovery needs coordinator wrapper, startup scan/reconciliation of stuck running/failed rows, and deliberate fail test; later demonstration claims injected running row is reconciled to failed then resumed with appended update; source notes coordinator was run manually rather than wired automatically; source prefers an always-invoked coordination instruction over an explicitly invoked skill for pre-dispatch behavior; source identifies awkward/mixed phases and recommends removing duplicate `results JSON` approach in favor of scratchpad/synthesis flow (16469-16569).
- Candidate objectives: `ccaf-cNN-o18` (NN TBD): Evaluate an agent orchestration design beyond happy-path completion. `ccaf-cNN-o19` (NN TBD): Specify startup reconciliation and resume handling for interrupted task state. `ccaf-cNN-o20` (NN TBD): Detect duplicated orchestration artifacts or phases that create conflicting control paths.
- Glossary terms with g- slugs: g-happy-path; g-failure-injection; g-stuck-task; g-task-reconciliation; g-resume-flow; g-startup-scan; g-coordinator-invocation; g-skill-invocation; g-control-path-duplication.
- Diagram candidates with archetype from concept|process|comparison|decision|worked-scenario|callout|diagram|mcq-set|active-recall: process: startup -> scan manifest -> reconcile running/failed state -> dispatch/resume -> append finding update; decision: automatic pre-dispatch coordinator instruction versus manually invoked skill; comparison: scratchpad-plus-synthesis versus parallel results-JSON storage.
- Comparison/decision table candidates with rows and columns: Rows: happy-path-only design, startup reconciliation, manual coordinator command, always-required pre-dispatch coordinator instruction, duplicate results JSON plus scratchpads. Columns: interruption handling, automatic invocation, state source, duplication risk, source assessment.
- UNVERIFIED: MCQ candidates (count + applied stem ideas): 4. Parent dies while manifest says running; choose startup behavior. Test only proves normal completion; identify missing validation. Coordinator exists but is never automatically called; identify operational gap. Select design that avoids duplicate result representation noted in source.
- UNVERIFIED: Recall prompts (count + prompts): 4. What failure-handling elements does source say were missing initially? What does reconciliation do to stuck task state in described test? Why is manual coordinator invocation a gap? Which artifact approach did source prefer after identifying overlap?
- Confusion pairs with discriminator: resume versus retry: resume continues stateful interrupted task; retry reattempts validation correction; skill versus standing coordination instruction: source says skill may require explicit invocation while coordination must happen before every dispatch.
- UNVERIFIED: Common wrong turn misconceptions: Append-only artifacts alone guarantee recovery. A smoke test proves crash recovery. A manually run coordinator is equivalent to automatic pre-dispatch enforcement. Parallel state formats improve clarity without cost.
- Causal step chains only if genuine, else 'none - omission beats invented process': 1. Parent interruption leaves manifest row running/failed. 2. Startup coordinator scans task rows. 3. Reconciliation marks/identifies stuck work. 4. Dispatcher resumes required task. 5. Resumed subagent appends progress to preserved findings.

### 8. Provenance, observability, and rate-limit diagnosis
- Key concepts: provenance; original sources; tool use; hallucinated sources; progress logging; opaque waits; parallel dispatch; token-rate limit; serial exploration; max usage; incomplete persistence; phase simplification.
- UNVERIFIED: Exam-relevant facts: Source observes synthesized film-research output lacked original sources and concludes intended tools had not actually been used, creating hallucination risk; then adds logging because web-search progress appeared frozen; concurrent explorations hit a 50,000 token input-per-second rate limit; source reduces work to one axis/one exploration; later synthesis reports completion but files are empty because max usage was hit; source describes simplifying overengineered, mixed phases (14729-15303).
- Candidate objectives: `ccaf-cNN-o21` (NN TBD): Verify provenance by checking whether claimed sources/tool outputs were actually preserved and used. `ccaf-cNN-o22` (NN TBD): Diagnose apparent agent stalls with meaningful runtime observability. `ccaf-cNN-o23` (NN TBD): Adapt dispatch concurrency when rate limits or usage ceilings prevent durable completion. `ccaf-cNN-o24` (NN TBD): Distinguish completion messaging from persisted artifact completion.
- Glossary terms with g- slugs: g-provenance; g-source-preservation; g-tool-execution; g-hallucinated-source; g-runtime-observability; g-progress-logging; g-rate-limit; g-concurrency; g-usage-ceiling; g-durable-completion.
- Diagram candidates with archetype from concept|process|comparison|decision|worked-scenario|callout|diagram|mcq-set|active-recall: worked-scenario: dispatch parallel axes -> token-rate limit -> reduce to single axis -> inspect artifacts; decision: completion message plus empty files -> treat as incomplete and inspect usage/error state; comparison: claimed source citations without tool execution versus preserved source provenance with actual tool result.
- Comparison/decision table candidates with rows and columns: Rows: parallel multi-axis run, single-axis run, tool-free claimed research, actual tool-backed research, completion message with empty artifact. Columns: observed signal, source/provenance confidence, rate-limit exposure, artifact verification required, corrective action.
- UNVERIFIED: MCQ candidates (count + applied stem ideas): 5. Synthesized report has specific sources but no tool call/provenance; choose assessment. Console is silent during search; select observability improvement. Parallel subagents hit input token rate limit; choose immediate scope/concurrency response. Run says synthesis complete but output folder empty after usage limit; classify state. Choose purpose of simplifying phases after mixed approaches conflict.
- UNVERIFIED: Recall prompts (count + prompts): 5. What source observation revealed provenance failure? Why add logs in the described run? What resource limit was hit? What did source reduce to after rate limit? Why must persisted files be checked after completion message?
- Confusion pairs with discriminator: provenance citation versus provenance preservation: citation text can be hallucinated; preservation ties claim to actual source/tool result; rate limit versus max usage: rate limit blocks request throughput, max usage left no persisted files in described later run; logs versus findings: logs expose runtime progress, findings are knowledge artifacts.
- Common wrong turn misconceptions: Specific-looking cited sources prove web research occurred. A quiet run is necessarily frozen. More parallel agents always finish sooner. Completion output proves artifact writes succeeded. Adding phases fixes an overengineered workflow.
- Causal step chains only if genuine, else 'none - omission beats invented process': 1. Parallel dispatch starts multiple exploration requests. 2. Input token rate limit stops progress. 3. Reduce concurrency/scope to one exploration axis. 4. Log ongoing operations to distinguish work from stall. 5. Inspect persisted scratchpads/synthesis, not status wording alone, before declaring success.

## Proposed chapter mapping
- Provisional chapter: `ccaf-cNN` (title TBD: Reliable agent workflows and context management). Do not assign final chapter number until all ten transcript plans are consolidated against exam blueprint.
- Lesson 1 maps to review architecture and output quality control.
- Lesson 2 maps to reliability controls: validation, remediation, bounded retries, and terminal conditions.
- Lessons 3-4 map to context engineering: preservation of precision, section handling, external memory, and tool-result reduction.
- Lessons 5-7 map to multi-agent orchestration: coordinator isolation, durable artifacts, role contracts, recovery, and simplified control flow.
- Lesson 8 maps to operational assurance: provenance, logs, rate limits, usage limits, and artifact verification.
- UNVERIFIED: Candidate chapter assessment: strong cross-cutting architecture chapter; split into `Context management` and `Reliable multi-agent orchestration` only if other chunks materially deepen either area.

## Hands-on / lab content
- Lab 1: Review boundary design. Given generated multiple-choice questions and a reviewer requirement, draw self-review and fresh-context review flows; identify retained history and state why independent context is selected. Evidence: 15304-15488.
- Lab 2: Validation remediation trace. Given a structured triage result with a specific business-rule failure, trace validator feedback, corrected retry, max-attempt terminal state, and absent-source stop decision. Evidence: 15489-15670.
- Lab 3: Context budget triage. Given a long report, a list of exact numbers/dates, and a large mock tool result, classify facts to preserve externally, sections to process, and fields to pass onward. Evidence: 15671-15929.
- Lab 4: Durable exploration architecture. On paper, define coordinator, explorer, synthesizer, manifest, and append-only scratchpad responsibilities; include interruption/resume path and prohibited source modification. Evidence: 15930-16468.
- Lab 5: Failure review. Given run signals: no source provenance, no visible progress, rate-limit response, and completion message with empty files, diagnose each separately and name verification artifact. Evidence: 14729-15303; 16469-16569.
- No implementation lab mandated by transcript plan. If implemented later, preserve repository constraints: static offline pages, ASCII-only source, no dependencies, no remote assets, no inline styles, shared presentation assets, classic scripts only, and verifier pass.

## Open questions and gaps
- Exact exam-domain placement and final chapter numbers remain unknown; all `ccaf-cNN-oM` IDs are provisional.
- Transcript presents examples and instructor interpretation, not official exam objectives; map claims to official CCA-F blueprint before final registry wording.
- Source says independent review reduces anchoring risk, but explicitly does not establish that reviewer output is correct; plan should preserve this boundary.
- Source briefly questions whether Pydantic/Instructor could handle some validations, but does not verify capabilities; do not author tool-specific claims from this chunk.
- No rigorous metrics compare self-review and independent review in transcript.
- Lost-in-the-middle mechanism is described operationally, not formally characterized; avoid unsupported guarantees about model attention behavior.
- Demo says external facts are stored in a file after execution, but does not specify a general persistence format, durability model, or security controls.
- Orchestration examples contain iterative, imperfect implementation and later discovered gaps; teach observed design lessons, not code as canonical architecture.
- Agent definition precedence/file-loading behavior is raised as a concern but not conclusively resolved in transcript.
- Provenance implementation remains incomplete in source narrative: initial system did not call tools despite expected sourcing; later corrected run hit limits before durable output; no successful end-to-end provenance evidence shown.
- Rate-limit value is an observed run condition, not portable platform guidance; retain only as scenario evidence.
- No final successful film-research artifact is shown after phase simplification and rate-limit/max-usage issues; do not claim final design works.

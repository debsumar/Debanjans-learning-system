# CCAF planning folder

This folder plans Claude Certified Architect - Foundations.
Slug: `ccaf`.
Transcript-used code: `CCA-F`.
Official code, title, domains, weights, and objective wording remain UNVERIFIED.

`chunk-01.plan.md` through `chunk-10.plan.md` are source-local evidence.
Each preserves transcript lesson boundaries, citations, candidate objectives, labs, and gaps.
They are not implementation instructions or official exam evidence.

`PLAN.md` is cross-chunk master synthesis.
It maps all 84 planned lessons into 13 provisional future chapters.
It defines draft registry layers, question selection, diagrams, glossary scope, models, review, and verifier risks.
Chunk files can feed several chapters; chapter boundaries follow learning decisions, not chunk boundaries.

Source orientation and study method now map to C01 objectives.
Middle chunks feed orchestration, prompting, reliability, evidence, sessions, settings, and safety.
Later chunks feed MCP, diagnostics, automation, batch work, and escalation.
`PLAN.md` contains the full lesson-to-chapter index.

Nothing is implemented.
No `topics/ccaf/` folder exists.
No HTML, CSS, JavaScript, registry, launcher, or shared asset is changed by this planning stage.
No chunk plan is modified by this synthesis task.

Future order: official guide -> registry layers -> topic token file -> static pages -> launcher -> verification.
Future implementation must remain offline-first, ASCII-only, relative-path-only, static, and readable without JavaScript.
Run `tools/verify.ps1` and require zero failures before commit.
## Chunk metadata

- Chunk file: `/tmp/ccaf-chunks/chunk1`.
- Source transcript range: course lines 1842-3682; chunk-local lines 1-1841.
- Rough course position: middle course segment on tool-use response handling, code-driven versus model-driven decision-making, agent-loop termination, coordinator/hub-and-spoke design, and narrow decomposition. Chunk begins mid tool-use walkthrough and ends mid narrow-decomposition walkthrough [1-1841].
- Scope boundary: planning evidence only. No CCA-F (UNVERIFIED) blueprint, official objective list, chapter titles, or score weighting appears in this chunk [1-1841].

## Lesson inventory

| # | Lesson/segment title inferred from transcript | Chunk line refs | One-line summary |
|---:|---|---|---|
| 1 | Tool-use response handling and tool-result return | 1-390 | Inspect a `tool_use` (UNVERIFIED) response, dispatch matching tool code, return a correlated `tool_result` (UNVERIFIED), then make a follow-up request [1-390]. |
| 2 | Preconfigured decision trees versus model-driven decisions | 390-620 | Contrast code-controlled `if`/state-machine paths with an agentic model selecting tool sequence from context [390-620]. |
| 3 | Code-driven/model-driven routing and safe agent-loop termination | 620-1000 | Demonstrate structured classification routing, model-selected inventory tools, and termination based on `stop_reason` (UNVERIFIED) plus a maximum-iteration cap [620-1000]. |
| 4 | Hub-and-spoke coordinator architecture | 1000-1115 | Define coordinator-owned routing, context sharing, error handling, observability, decomposition, delegation, and aggregation [1000-1115]. |
| 5 | Coordinator-agent job-screening walkthrough | 1115-1500 | Build and inspect a coordinator that routes a job posting and resume through independent screening spokes and aggregates their outputs [1115-1500]. |
| 6 | Narrow task decomposition | 1501-1841 | Show how isolated subtasks can omit whole research dimensions and identify review-before-delegation and aggregation-stage safeguards [1501-1841]. |

## Per-lesson plan

### 1. Tool-use response handling and tool-result return

- Key concepts: tool schema/property input; `tool_choice` (UNVERIFIED) set to `auto` (UNVERIFIED); `tool_use` (UNVERIFIED) content block; `stop_reason` (UNVERIFIED); tool-name dispatch; tool input; `tool_result` (UNVERIFIED); `tool_use_id` (UNVERIFIED); original versus follow-up response [1-390].
- Exam-relevant facts: A response can have `stop_reason` (UNVERIFIED) `tool_use` (UNVERIFIED), meaning Claude reasoned that it should call a tool; the tool-use block contains tool information and extracted inputs [55-135]. A handler locates the tool-use block, checks its name, calls matching tool code with input, then sends the tool result in a follow-up message [210-352]. `tool_result` (UNVERIFIED) includes a `tool_use_id` (UNVERIFIED), enabling the result to be mapped to the specific tool call; the follow-up shown ends the turn [330-390]. A loop can run forever when the response is never updated inside it [160-205].
- Candidate objectives: `ccaf-cTBD-o1` - Trace one tool-use turn from model response through dispatch, `tool_result` (UNVERIFIED), and follow-up response [55-390]. `ccaf-cTBD-o2` - Identify why a tool-use loop must receive an updated response before reevaluating its termination condition [160-205].
- Glossary terms: `tool use` - `g-tool-use`; `tool choice` - `g-tool-choice`; `stop reason` - `g-stop-reason`; `tool result` - `g-tool-result`; `tool use ID` - `g-tool-use-id`; `follow-up request` - `g-follow-up-request` [1-390].
- Diagram candidates: `process` - response with `tool_use` (UNVERIFIED) -> extract name/input -> run matching tool -> return `tool_result` (UNVERIFIED) with `tool_use_id` (UNVERIFIED) -> follow-up response [210-390]. `callout` - `stop_reason: tool_use` (UNVERIFIED) signals tool invocation rather than completed answer [108-135].
- Table candidates: rows `response condition: tool_use block found`, `no relevant tool-use block`, `follow-up end_turn`; columns `handler action`, `message content sent`, `next response expected` [210-390].
- MCQ candidates: 3. A returned `tool_result` (UNVERIFIED) has an incorrect correlation identifier: choose required correction [330-360]. A handler sees `stop_reason` (UNVERIFIED) `tool_use` (UNVERIFIED): choose next operation [108-135]. A loop retains its initial response forever: identify defect [160-205].
- Recall prompts: 4. What fields let application code identify and invoke requested tool work? What correlates a tool result to a tool call? Why must a loop update `response`? What outcome was observed in the follow-up response? [55-390].
- Confusion pairs: `tool_use` (UNVERIFIED) vs `tool_result` (UNVERIFIED) - former requests/exposes a tool call; latter returns its execution output to the conversation [55-135,330-360]. `tool name` vs `tool_use_id` (UNVERIFIED) - name selects handler; ID correlates returned result with specific invocation [260-360].
- Common wrong turn: `A printed response alone completes tool use -> application must execute requested tool and send a result back before model can continue` [108-390]. `A loop stops automatically when tool work is printed -> it can run forever if its response state is not updated` [160-205].
- Causal steps: 1. Receive response with `tool_use` (UNVERIFIED) [108-135]. 2. Locate tool-use block and extract tool name/input [210-290]. 3. Invoke matching tool function [290-330]. 4. Construct/send `tool_result` (UNVERIFIED) carrying its `tool_use_id` (UNVERIFIED) [330-360]. 5. Receive follow-up response and inspect that it ended the turn [360-390].

### 2. Preconfigured decision trees versus model-driven decisions

- Key concepts: preconfigured decision tree; code-driven decision-making; `if`/`else`; state machine; tool sequence; agentic loop; model-driven decision-making; runtime context [390-620].
- Exam-relevant facts: Preconfigured decision trees hardcode logic and code decides what happens next; an `if`/`else` is a simple example, while a state machine defines an initial state and state transitions [390-440]. The transcript equates a tool sequence with a state machine or series of steps [420-440]. If code makes the decision, it is described as not exactly agentic; an agentic loop has the agent make each decision [440-480]. Model-driven decision-making gives Claude tools and a goal, and Claude figures out sequence at runtime based on context; the caller does not specify which tool to call or when [501-530]. Both preconfigured and model-driven options are stated to be valid and useful depending on task [540-560].
- Candidate objectives: `ccaf-cTBD-o3` - Distinguish code-driven decision trees from model-driven, agentic decision-making [390-530]. `ccaf-cTBD-o4` - Select decision ownership appropriate to a stated workflow constraint [390-560].
- Glossary terms: `preconfigured decision tree` - `g-preconfigured-decision-tree`; `code-driven decision-making` - `g-code-driven-decision-making`; `state machine` - `g-state-machine`; `tool sequence` - `g-tool-sequence`; `agentic loop` - `g-agentic-loop`; `model-driven decision-making` - `g-model-driven-decision-making` [390-560].
- Diagram candidates: `comparison` - code-driven: code selects next action; model-driven: Claude selects sequence at runtime from goal/context/tools [390-530]. `concept` - decision owner as central discriminator: code versus agent [440-530].
- Table candidates: rows `decision owner`, `tool/action order`, `mechanism named in transcript`, `agentic status as described`; columns `preconfigured/code-driven`, `model-driven` [390-530].
- MCQ candidates: 3. A workflow needs code to select every next transition: classify approach [390-440]. A developer gives tools and a goal but no fixed call order: classify approach [501-530]. Select transcript-supported reason that both approaches can be useful [540-560].
- Recall prompts: 3. Define preconfigured decision tree from transcript. When is a state-machine/tool-sequence description used? What decision ownership makes a loop agentic in this lesson? [390-530].
- Confusion pairs: `state machine/tool sequence` vs `agentic loop` - former uses code-specified transitions/steps; latter has agent decide each turn [420-480]. `model-driven` vs `tool-enabled but code-driven` - tools alone do not establish who decides next action [390-530].
- Common wrong turn: `Any multi-step tool sequence is agentic -> transcript says code-controlled decisions are not exactly agentic` [420-480]. `Model-driven decisions always replace preconfigured logic -> both are valid depending on work` [540-560].
- Causal steps: none - omission beats invented process.

### 3. Code-driven/model-driven routing and safe agent-loop termination

- Key concepts: constrained classifier output; hard-coded route; inventory-tool schema; model-selected tool calls; `stop_reason` (UNVERIFIED); `end_turn` (UNVERIFIED); natural-language completion parsing; text output; maximum iteration/max steps; break [620-1000].
- Exam-relevant facts: Code-driven example asks a customer-support classifier to return exactly one of billing, technical, or general, then routes each category to hard-coded handling [640-740]. The example uses structured/specific category output so code can act on it [700-740]. It is offered for logic/state constrained by code, with flexibility while limiting hallucination and token consumption in a text-adventure-style example [740-790]. Model-driven inventory example exposes `check inventory` (UNVERIFIED), `place order` (UNVERIFIED), and `send notifications` (UNVERIFIED); its prompt says to verify inventory before ordering [800-885]. Tool descriptions/schemas are said to let the agent reason about tools, so explicitly repeating tool names in the system prompt is not needed in that demo [820-875]. Do not end an agent loop by parsing natural-language text or merely seeing text output; use `stop_reason` (UNVERIFIED)/`end_turn` (UNVERIFIED), because text can be misleading [900-955]. A maximum iteration cap can prevent overuse/runaway work, but should combine with `stop_reason` (UNVERIFIED) and a break so work can stop earlier when complete [940-985].
- Candidate objectives: `ccaf-cTBD-o5` - Design a code-driven route using constrained model classification output [640-740]. `ccaf-cTBD-o6` - State safe completion control for a tool-use agent loop and role of an iteration cap [900-1000]. `ccaf-cTBD-o7` - Explain inventory-before-order constraint in the model-driven tool example [800-885].
- Glossary terms: `structured output` - `g-structured-output`; `hard-coded routing` - `g-hard-coded-routing`; `end turn` - `g-end-turn`; `maximum iteration` - `g-maximum-iteration`; `max steps` - `g-max-steps`; `natural language parsing` - `g-natural-language-parsing` [640-1000].
- Diagram candidates: `decision` - classified support category -> billing/technical/general hard-coded handler [640-740]. `process` - model asks for inventory tool -> application returns result -> model may select next tool -> `end_turn` (UNVERIFIED) exits [800-1000]. `callout` - completion signal: structured `stop_reason` (UNVERIFIED), not phrase matching [900-955].
- Table candidates: rows `completion signal`, `can text mislead?`, `runaway guard`, `what determines next action`; columns `parse natural language`, `text present`, `stop_reason plus max iteration` [900-1000]. Rows `billing`, `technical`, `general`; columns `classifier output`, `hard-coded handler` [640-740].
- MCQ candidates: 4. Support classifier returns `technical`: select correct routing mechanism [640-740]. An inventory assistant wants to place order before stock check: select violated instruction [850-885]. A response includes text while tools may still be needed: select reliable exit signal [900-955]. A loop must stop both when complete and when it becomes runaway: select combined control [940-1000].
- Recall prompts: 5. List three support categories in example. Why constrain classifier response to one category? What must occur before ordering in inventory example? Why is text completion brittle? Why combine max iteration with `stop_reason` (UNVERIFIED)? [640-1000].
- Confusion pairs: `structured category output` vs `natural-language completion text` - former is constrained for route handling; latter is brittle as loop-ending evidence [700-740,900-955]. `max iteration` vs `stop_reason` (UNVERIFIED) - cap bounds runaway consumption; `stop_reason` (UNVERIFIED) permits timely normal completion [940-985]. `end_turn` (UNVERIFIED) vs text content - structured end indication versus potentially unrelated/misleading text [900-955].
- Common wrong turn: `Any returned text proves no more tools are needed -> text may appear in either case; inspect stop reason/end turn` [900-955]. `A fixed ten-iteration loop is sufficient -> use cap with stop reason/break so completed work stops sooner` [940-985]. `Model must be told tool names in system prompt -> demo says API tool specifications/descriptions enable agent reasoning` [820-875].
- Causal steps: 1. Send constrained classification request [640-720]. 2. Receive one category [680-740]. 3. Route category/message to code-owned handler [720-740]. Separate tool loop: 1. Model requests tool use [800-900]. 2. Process tool work and obtain next response [880-920]. 3. Check `stop_reason` (UNVERIFIED)/`end_turn` (UNVERIFIED) [900-955]. 4. Break when ended, while max iteration bounds a runaway loop [940-1000].

### 4. Hub-and-spoke coordinator architecture

- Key concepts: coordinator agent/hub; subagents/spokes; no direct spoke-to-spoke communication; coordinator routing/context sharing/error handling/observability; decomposition; delegation; aggregation; sequential versus parallel delegation [1000-1115].
- Exam-relevant facts: In hub-and-spoke architecture, one coordinator is at center and all subagents talk to it [980-1020]. A research agent cannot directly talk to reviewer agent; communication goes through coordinator [1010-1035]. Coordinator owns routing, what context is shared, error handling, and observability; centralized traffic creates an observability choke point [1035-1060]. Coordinator decomposes task into subtasks, delegates work, aggregates results, and decides which subagents to invoke based on query complexity [1060-1080]. Lesson guidance: simple factual questions use one agent; multi-step tasks may pass results sequentially; independent subtasks may delegate in parallel; coordinator combines outputs and resolves conflicts [1080-1115].
- Candidate objectives: `ccaf-cTBD-o8` - Describe coordinator responsibilities and communication boundary in hub-and-spoke architecture [1000-1080]. `ccaf-cTBD-o9` - Choose single-agent, sequential, or parallel delegation from task dependency [1080-1115].
- Glossary terms: `hub and spoke architecture` - `g-hub-and-spoke-architecture`; `coordinator agent` - `g-coordinator-agent`; `subagent` - `g-subagent`; `spoke` - `g-spoke`; `task decomposition` - `g-task-decomposition`; `task delegation` - `g-task-delegation`; `result aggregation` - `g-result-aggregation`; `observability` - `g-observability` [1000-1115].
- Diagram candidates: `diagram` - coordinator hub with research/reviewer spokes and all arrows through hub [1000-1060]. `decision` - factual -> one agent; dependent multi-step -> sequential; independent -> parallel [1080-1115].
- Table candidates: rows `routing`, `context sharing`, `error handling`, `observability`, `result ownership`; columns `coordinator`, `individual spoke` [1010-1080]. Rows `simple factual`, `dependent multi-step`, `independent subtasks`; columns `delegation pattern`, `result movement` [1080-1115].
- MCQ candidates: 3. Research and review workers need to exchange context: identify permitted path [1010-1035]. Independent subtasks arrive: choose delegation mode [1080-1115]. Select responsibility that remains with coordinator rather than spoke [1035-1080].
- Recall prompts: 4. What communication is forbidden between spokes? Name four coordinator-owned concerns. What creates observability choke point? When is sequential delegation appropriate? [1010-1115].
- Confusion pairs: `coordinator` vs `spoke` - coordinator orchestrates/routs/shares/aggregates; spoke executes assigned work without direct peer lines [1010-1080]. `sequential` vs `parallel` delegation - dependency/result forwarding versus independent subtasks [1080-1115].
- Common wrong turn: `Subagents should share context directly for speed -> transcript architecture sends communication/context through coordinator` [1010-1050]. `Coordinator should perform every subtask -> lesson says decompose/delegate and aggregate` [1060-1115].
- Causal steps: 1. Coordinator receives task [1060-1080]. 2. Decomposes it into subtasks [1060-1080]. 3. Chooses spokes/delegation pattern based on complexity/dependency [1060-1115]. 4. Shares needed context and receives outputs through hub [1010-1080]. 5. Aggregates, resolves conflicts, and produces coherent response [1080-1115].

### 5. Coordinator-agent job-screening walkthrough

- Key concepts: simple coordinator versus task-manager skeleton; concrete/validatable use case; job posting/resume; keyword scanner; deep evaluator; red-flag detector; dispatch schema; coordinator prompt; independent spokes; fixed order; aggregation; max steps [1115-1500].
- Exam-relevant facts: Initial basic coordinator creates/checks/completes/lists tasks but is judged too generic for the intended decomposition-routing-aggregation pattern [1120-1190]. In proper hub-and-spoke, coordinator is hub: it receives request, calls spokes, and aggregates results; decomposer is not a separate subagent, while independent workers each do one job and lack knowledge of each other [1260-1320]. Job-screening example uses keyword scanner, deep evaluator, red-flag detector, and score aggregator; coordinator owns invoking right spoke, deciding which to call, collecting, and combining outputs [1320-1360]. Keyword-scanner prompt checks whether required job-posting skills appear explicitly in resume, reports one line per skill, and must not infer/extrapolate [1360-1390]. Coordinator prompt says orchestrate three independent screening agents, run three in order, and do not skip any [1410-1445]. Demo introduces a max-steps cap of 10, described as double expected five steps to allow retries [1450-1490].
- Candidate objectives: `ccaf-cTBD-o10` - Evaluate whether a proposed coordinator actually owns decomposition, routing, and aggregation [1120-1320]. `ccaf-cTBD-o11` - Map job-screening functions to independent spokes and coordinator responsibilities [1320-1445]. `ccaf-cTBD-o12` - Apply literal evidence boundary to a keyword-scanning spoke [1360-1390].
- Glossary terms: `keyword scanner` - `g-keyword-scanner`; `deep evaluator` - `g-deep-evaluator`; `red flag detector` - `g-red-flag-detector`; `score aggregator` - `g-score-aggregator`; `dispatch tool` - `g-dispatch-tool`; `explicit order` - `g-explicit-order`; `literal evidence` - `g-literal-evidence` [1320-1490].
- Diagram candidates: `worked-scenario` - job posting/resume -> coordinator -> keyword scanner/deep evaluator/red-flag detector -> aggregate hiring recommendation [1320-1445]. `comparison` - generic task manager versus coordinator with decomposition/routing/aggregation [1120-1260]. `callout` - literal scanner rule: report explicitly stated skills; do not infer/extrapolate [1360-1390].
- Table candidates: rows `create/check/complete task`, `decomposition`, `complexity/routing`, `spoke invocation`, `aggregation`; columns `basic task-manager skeleton`, `hub-and-spoke coordinator` [1120-1320]. Rows `keyword scanner`, `deep evaluator`, `red-flag detector`, `score aggregator`; columns `input stated`, `output responsibility`, `coordinator relation` [1320-1445].
- MCQ candidates: 4. A decomposer is implemented as peer spoke: identify hub-ownership violation [1260-1320]. Resume does not explicitly state claimed skill: choose keyword scanner output behavior [1360-1390]. Coordinator has three ordered screening spokes: choose correct execution treatment [1410-1445]. Expected workflow is five steps but retries can occur: identify transcript's cap rationale [1450-1490].
- Recall prompts: 5. Why was generic task-management coordinator insufficient? Who owns decomposition in proper hub-and-spoke? Name job-screening spokes. What evidence may keyword scanner report? What did max steps protect against? [1120-1490].
- Confusion pairs: `decomposer` vs `coordinator` - coordinator owns decomposition; decomposer is not separate subagent in proper pattern [1260-1320]. `keyword scanner` vs `deep evaluator` - scanner reports explicit required skills; evaluator is separately named screening agent [1320-1390]. `independent spokes` vs `ordered execution` - spokes lack peer knowledge, while coordinator prompt can require execution in explicit order [1260-1445].
- Common wrong turn: `A task list alone demonstrates coordinator architecture -> intended coordinator also decomposes, routes, and aggregates` [1120-1260]. `Keyword scanner should infer unstated capability -> prompt says report only explicitly stated evidence` [1360-1390]. `Fixed order means spokes communicate directly -> coordinator still orchestrates independent spokes` [1260-1445].
- Causal steps: 1. Coordinator receives job posting and resume [1410-1450]. 2. It invokes keyword scanner, deep evaluator, and red-flag detector in specified order [1320-1445]. 3. Each worker produces its assigned screening output without peer knowledge [1260-1390]. 4. Coordinator collects outputs and score aggregation produces recommendation [1320-1360]. 5. Max steps bounds retries/runaway execution [1450-1490].

### 6. Narrow task decomposition

- Key concepts: narrow initial decomposition; isolated subagent context; omitted research dimensions; task-specific coverage criteria; pre-delegation breakdown review; aggregation-stage coverage review; research coordinator versus hiring example limitation [1501-1841].
- Exam-relevant facts: Claude can delegate only what it thinks to ask for; if initial decomposition is too narrow, entire topics never get researched [1501-1545]. Example EV-market subtasks cover sales figures, battery technology, and manufacturers but omit charging infrastructure, government policies/subsidies, secondhand markets, consumer sentiment/adoption barriers, lithium/cobalt supply chains, and grid-capacity implications [1510-1570]. Each subagent sees isolated context, so none can flag missing topics [1540-1560]. A coordinator can guard before delegation by reviewing subtask breakdown against stated considerations, or after aggregation by checking coverage before final answer [1570-1625]. Job-screening example may not fit broad research decomposition cleanly; transcript discusses need for multiple information sources and specific considerations, but the attempted adaptation remains unresolved [1625-1841].
- Candidate objectives: `ccaf-cTBD-o13` - Detect omission risk created by narrow coordinator decomposition [1501-1570]. `ccaf-cTBD-o14` - Place decomposition-coverage safeguards before delegation and/or before final aggregated response [1570-1625]. `ccaf-cTBD-o15` - Explain why isolated subagents cannot independently detect unassigned topic gaps [1540-1560].
- Glossary terms: `narrow task decomposition` - `g-narrow-task-decomposition`; `isolated context` - `g-isolated-context`; `coverage gap` - `g-coverage-gap`; `pre-delegation review` - `g-pre-delegation-review`; `aggregation-stage review` - `g-aggregation-stage-review`; `subtask breakdown` - `g-subtask-breakdown` [1501-1625].
- Diagram candidates: `worked-scenario` - EV request -> narrow three-subtask plan -> visible omitted dimensions -> pre-delegation/aggregation safeguards [1510-1625]. `decision` - review location: before delegation versus after aggregation [1570-1625].
- Table candidates: rows `sales`, `battery technology`, `manufacturers`, `charging infrastructure`, `policy/subsidies`, `secondhand market`, `consumer barriers`, `supply chains`, `grid capacity`; columns `initial EV decomposition`, `identified as omitted` [1510-1570]. Rows `review timing`, `input reviewed`, `goal`; columns `before delegation`, `after aggregation` [1570-1625].
- MCQ candidates: 3. EV research plan assigns sales/battery/manufacturers only: identify unmitigated coordinator risk [1510-1570]. A worker finds no task about grid capacity: identify why it cannot necessarily correct omission [1540-1560]. Choose safeguard that checks final coverage before answer writing [1570-1625].
- Recall prompts: 4. Why cannot isolated subagents flag missing research topics? Name three omitted EV dimensions. What safeguard occurs before delegation? What safeguard occurs after aggregation? [1501-1625].
- Confusion pairs: `task delegation` vs `task decomposition` - delegation assigns created subtasks; decomposition determines whether needed topics were created at all [1501-1625]. `pre-delegation review` vs `aggregation-stage review` - first checks breakdown before worker assignment; second checks assembled answer coverage [1570-1625]. `narrow worker scope` vs `inadequate overall coverage` - a worker may correctly execute isolated task while coordinator omits adjacent domain [1501-1560].
- Common wrong turn: `More subagents automatically make research comprehensive -> omitted topics remain unresearched if coordinator never delegates them` [1501-1560]. `A subagent can detect all missing work -> it sees only isolated context` [1540-1560]. `One review point is mandated -> transcript presents pre-delegation and aggregation-level safeguards` [1570-1625].
- Causal steps: 1. Coordinator receives broad task [1501-1545]. 2. Initial decomposition creates limited subtasks [1510-1545]. 3. Isolated workers research only assigned scope [1540-1560]. 4. Unassigned dimensions remain absent [1510-1570]. Mitigation path: 1. Review breakdown against required considerations before delegation [1570-1605]. 2. Delegate work [1605-1625]. 3. Aggregate results [1605-1625]. 4. Review coverage before final answer [1605-1625].

## Proposed chapter mapping

| Lesson | Proposed CCA-F (UNVERIFIED) chapter/domain | Mapping status and continuation |
|---|---|---|
| 1. Tool-use response handling and tool-result return | TBD - Agent/tool-use execution lifecycle | Draft domain only; chapter number unavailable in source. Self-contained practical segment [1-390]. |
| 2. Preconfigured versus model-driven decisions | TBD - Agentic system design and orchestration | Draft domain only; conceptual segment continues into code/model routing lesson [390-620]. |
| 3. Routing and safe loop termination | TBD - Agent/tool-use execution lifecycle | Draft domain only; connects decision ownership with runtime loop controls [620-1000]. |
| 4. Hub-and-spoke coordinator architecture | TBD - Multi-agent orchestration | Draft domain only; architecture immediately applied in lesson 5 [1000-1115]. |
| 5. Coordinator-agent job-screening walkthrough | TBD - Multi-agent orchestration | Draft domain only; implementation example, not proof of official exam requirement [1115-1500]. |
| 6. Narrow task decomposition | TBD - Multi-agent orchestration and quality controls | Continues into next chunk; transcript ends during attempted hiring-coordinator adaptation [1501-1841]. |

## Hands-on / lab content

| Segment | Hands-on/demo activity | Exam-relevant plan extraction | Demo-only or unresolved material |
|---|---|---|---|
| Tool-use response walkthrough [1-390] | Define one magic-eight-ball-style tool, set `tool_choice` (UNVERIFIED) `auto` (UNVERIFIED), print/pretty-print response, locate `tool_use` (UNVERIFIED), invoke tool, send `tool_result` (UNVERIFIED), inspect follow-up [1-390]. | Tool-use response lifecycle, correlation via `tool_use_id` (UNVERIFIED), need to update loop response [55-390]. | IDE caching, missing comma, formatting preference, function naming/refactoring commentary [70-105,210-330]. |
| Decision-making routing [620-885] | Generate/run customer-support classifier with billing/technical/general handlers; modify inventory tool example with check stock, order, notification tools [640-885]. | Code-owned route after constrained classification; model can select tool calls; stock checked before order in example [640-885]. | Folder names, UNVERIFIED: model-credit discussion, author preferences [620-640,790-820]. |
| Loop-ending demo [900-1000] | Copy model-driven code, replace unbounded loop behavior with break on end turn plus max-iteration 10 [900-1000]. | Use structured stop reason/end turn; cap runaway loop while permitting earlier completion [900-1000]. | Assertion that no test was needed; language/style opinions [980-1000]. |
| Basic and job-screening coordinators [1115-1500] | Generate generic task manager, revise toward coordinator; build job posting/resume screening through keyword/deep/red-flag spokes and aggregation, run with max steps [1115-1500]. | Coordinator ownership, literal keyword scanning, explicit ordered spokes, bounded execution [1260-1490]. | Specific fictional candidate result and author uncertainty about generated code/routing split [1120-1260,1450-1500]. |
| Narrow-decomposition adaptation [1501-1841] | Attempt to adapt hiring coordinator with extra data sources/self-reflection, then identify that example/domain adaptation is unresolved [1625-1841]. | Coverage-gap safeguards before delegation or after aggregation [1570-1625]. | Off-screen retrieval and failed/misaligned prompt changes; do not turn into normative course facts [1625-1841]. |

## Open questions and gaps

- Official CCA-F (UNVERIFIED) domains, chapter names, objective wording, and weighting are absent. Keep every `ccaf-cTBD-oM` ID chapter placeholder until certification manifest/source material establishes chapter map [1-1841].
- Transcript uses informal API/model discussion. Confirm official terminology for `stop_reason` (UNVERIFIED), `end_turn` (UNVERIFIED), `tool_use` (UNVERIFIED), `tool_result` (UNVERIFIED), and `tool_choice` (UNVERIFIED) against authoritative CCA-F (UNVERIFIED) documentation before publishing exam facts [1-1000].
- Tool schema visibility versus system-prompt tool naming is described as course/API behavior; validate exact platform guarantee and current SDK behavior before hardening claim [800-875].
- `end_turn` (UNVERIFIED) wording appears as spoken/transcribed terminology. Confirm exact stop-reason values and API response shape before authoring implementation-specific questions [900-1000].
- Coordinator job-screening example includes a hiring recommendation. Preserve only architecture mechanics; do not present automated hiring as a recommended or authoritative workflow [1115-1500].
- Determine whether later chunks resolve separate routing from decomposition, define additional coordinator safeguards, or finish narrow-decomposition lab before final chapter/page boundaries are assigned [1115-1841].
- Validate final plan against repository process before implementation: registry-first ordering, static/no-build architecture, ASCII source, relative paths, JS-disabled readability, shared presentation only, and zero failures from `tools/verify.ps1` are repository constraints, not transcript facts.

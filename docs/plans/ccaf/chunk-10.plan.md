## Chunk metadata
- UNVERIFIED: Topic: Claude Certified Architect - Foundations; slug: `ccaf`; exam code: `CCA-F`.
- Source: Andrew Brown course transcript, `/tmp/ccaf-chunks/chunk9`, local lines 1-1841; absolute transcript lines 16570-18407.
- Scope: final course material: skill/coordinator boundary, batch processing, MCP discovery, MCP resources versus tools, Claude Code GitHub Actions, and support-agent escalation.
- Evidence rule: claims below stay bound to cited transcript. Draft objective chapter number remains `NN` pending whole-topic registry synthesis.
- Authoring boundary: planning only; proposed static content must keep offline-first, ASCII-only, no-build, no-remote-asset, JS-disabled-readable contracts.

## Lesson inventory
| # | title | chunk line refs | one-line summary |
|---|---|---|---|
| 1 | Skills, coordinators, and procedure placement | chunk9:1-61 (16570-16630) | Procedures belong in skills; description supports direct invocation; coordinator guidance should be light. |
| UNVERIFIED: UNVERIFIED: 2 | Batch processing | chunk9:62-205 (16631-16774) | Batch submission saves cost but is asynchronous, may take up to 24 hours, and needs later result retrieval by batch ID. |
| UNVERIFIED: 3 | MCP discovery | chunk9:206-290 (16775-16859) | Configured MCP servers expose discovered tools at connection time as a flat agent-visible list. |
| UNVERIFIED: 4 | MCP resources versus tools | chunk9:291-471 (16860-17040) | Resources represent read-only data access; tools represent actions, despite both being implemented through callable server logic. |
| UNVERIFIED: 5 | Claude Code with GitHub Actions | chunk9:472-1286 (17041-17855) | Workflow triggers can invoke Claude Code from issues or PRs, but permissions, API-key setup, action latency, and PR workflow need deliberate validation. |
| 6 | Support-agent escalation | chunk9:1287-1841 (17856-18407) | Build explicit escalation criteria and state for support requests; escalate ambiguity and explicit human requests rather than guessing. |

## Per-lesson plan
### 1. Skills, coordinators, and procedure placement
- Key concepts: skill descriptions support direct invocation; skill contains procedure; coordinator is distinct from skill; project guidance should point lightly instead of duplicating procedure; session-credit exhaustion ended demonstration.
- UNVERIFIED: Exam-relevant facts: transcript states direct invocation can follow skill description; procedures live inside skill, not `CLAUDE.md`; course ends after this discussion (chunk9:1-61).
- Candidate objectives (draft wording + ID ccaf-cNN-oM): `ccaf-cNN-o1` - Distinguish a skill description, a skill procedure, and coordinator guidance based on their stated roles.
- Glossary terms with g- slugs: `g-skill`, `g-skill-description`, `g-direct-invocation`, `g-coordinator`, `g-procedure-placement`.
- Diagram candidates with archetype from concept|process|comparison|decision|worked-scenario|callout|diagram|mcq-set|active-recall: `comparison` - skill description -> invocation versus skill procedure -> execution guidance versus coordinator -> lightweight project routing.
- UNVERIFIED: Comparison/decision table candidates with rows and columns: columns `Artifact`, `Transcript-stated role`, `What not to place there`; rows `skill description`, `skill procedure`, `coordinator guidance`, `CLAUDE.md reference`.
- UNVERIFIED: MCQ candidates (count + applied stem ideas): 2. Given duplicated procedural text in project guidance, choose moving procedure into skill; given a phrase that must support direct invocation, choose skill description.
- UNVERIFIED: Recall prompts (count + prompts): 2. Where does transcript place procedure detail? What is description used for before a skill runs?
- Confusion pairs with discriminator: skill description versus skill procedure - description enables invocation; procedure supplies operational steps. Coordinator guidance versus skill - coordinator routes/scopes; skill owns procedure.
- Common wrong turn misconceptions: Put full procedure in coordinator guidance; treat skill description as documentation only; infer a completed demonstration after session credits end.
- Causal step chains only if genuine, else 'none - omission beats invented process': description supports direct invocation -> selected skill runs its procedure; transcript then separates light coordinator pointer from embedded procedure (chunk9:1-35).

### 2. Batch processing
- UNVERIFIED: Key concepts: batch processing; reduced cost; no immediate result; up-to-24-hour processing with no guaranteed SLA; request `custom_id`; batch creation; retrieve/check by saved batch ID; submit/check/run modes; blocking versus asynchronous workflow; no interactive tool-use round trips in batch.
- UNVERIFIED: Exam-relevant facts: presenter says batch can take up to 24 hours with no guaranteed SLA and is not for instant results; each submitted request has `custom_id`, and returned results use that ID; batch mode cannot perform interactive round trips, forcing a single tool call with blocked tool input; submit saves batch ID in a state file, check retrieves status using that ID; stated benefit is strong savings when results are not needed immediately (chunk9:62-205).
- UNVERIFIED: Candidate objectives (draft wording + ID ccaf-cNN-oM): `ccaf-cNN-o2` - Select batch processing when cost matters more than immediate response and describe asynchronous submission/result retrieval. `ccaf-cNN-o3` - Identify `custom_id` and persisted batch ID roles in correlating submitted work, status, and results. `ccaf-cNN-o4` - Explain why interactive tool-call round trips do not fit described batch execution.
- Glossary terms with g- slugs: `g-batch-processing`, `g-batch-id`, `g-custom-id`, `g-batch-state-file`, `g-asynchronous-processing`, `g-sla`, `g-interactive-round-trip`.
- UNVERIFIED: Diagram candidates with archetype from concept|process|comparison|decision|worked-scenario|callout|diagram|mcq-set|active-recall: `process` - requests with custom IDs -> batch create -> persist batch ID -> later check/retrieve -> correlate results by custom ID. `decision` - need result now? yes -> not batch; no -> batch for stated savings.
- Comparison/decision table candidates with rows and columns: columns `Mode`, `When result arrives`, `Interaction shape`, `State needed`, `Transcript-stated tradeoff`; rows `submit`, `check`, `run and wait/blocking`, `interactive request flow`.
- UNVERIFIED: MCQ candidates (count + applied stem ideas): 4. Pick workflow for low-urgency, cost-sensitive ticket set; identify key needed to map returned result to original request; choose reason a tool-using conversation cannot be interactive in batch; select safe command pattern for returning later instead of waiting.
- UNVERIFIED: Recall prompts (count + prompts): 4. What maximum processing time and SLA caveat does transcript give? What does submit persist? What does check use? What interaction limitation applies to batch tool use?
- UNVERIFIED: Confusion pairs with discriminator: `custom_id` versus batch ID - custom ID correlates individual request/result; batch ID identifies submitted batch for retrieve/check. Submit versus run-and-wait - submit returns control after saving state; run-and-wait blocks for completion.
- Common wrong turn misconceptions: Expect batch result instantly; leave caller waiting rather than persist ID and check later; design multi-turn interactive tool loop inside batch; assume a state file itself contains completed results.
- UNVERIFIED: Causal step chains only if genuine, else 'none - omission beats invented process': 1. Assign `custom_id` per request. 2. Create batch. 3. Save returned batch ID. 4. Later retrieve/check by batch ID. 5. Match results through custom IDs (chunk9:91-205).

### 3. MCP discovery
- Key concepts: multiple MCP servers; connection-time discovery/loading; agent sees flat list; source-server identity is not apparent in that flat list; server configuration option; individually installed servers; pseudo-code versus Anthropic/Agent SDK-specific code.
- UNVERIFIED: Exam-relevant facts: configured MCP servers have tools discovered and loaded at connection time; agent treats tools as a flat list with no awareness of originating server; transcript calls example pseudo-code rather than Anthropic or Agent SDK implementation; each server must be installed individually and configured through MCP-server option (chunk9:206-290).
- Candidate objectives (draft wording + ID ccaf-cNN-oM): `ccaf-cNN-o5` - Explain MCP discovery timing and the agent-visible flat tool-list consequence. `ccaf-cNN-o6` - Identify MCP-server configuration and installation prerequisites described for discovery.
- Glossary terms with g- slugs: `g-mcp`, `g-mcp-server`, `g-mcp-discovery`, `g-connection-time`, `g-flat-tool-list`, `g-tool-origin`, `g-pseudocode`.
- UNVERIFIED: Diagram candidates with archetype from concept|process|comparison|decision|worked-scenario|callout|diagram|mcq-set|active-recall: `concept` - MCP server A tools + MCP server B tools + MCP server C tools -> connection-time discovery -> one flat agent-visible tool list. `callout` - flat list lacks server-origin awareness.
- UNVERIFIED: Comparison/decision table candidates with rows and columns: columns `Aspect`, `Configured MCP server`, `Agent-visible discovered tool`; rows `installation`, `identity/source`, `loading time`, `selection context`.
- UNVERIFIED: MCQ candidates (count + applied stem ideas): 3. Diagnose agent confusion when same-named tools come from several servers; choose discovery timing; identify inaccurate claim that agent retains server-origin context in flat list.
- UNVERIFIED: Recall prompts (count + prompts): 3. When are multiple-server tools discovered? How does agent see discovered tools? What setup condition is stated for each server?
- UNVERIFIED: Confusion pairs with discriminator: MCP server versus discovered tool - server is configured/installed source; tool is flat-list capability visible to agent. Discovery versus explicit manual tool declaration - transcript describes automatic loading at connection time after server configuration.
- Common wrong turn misconceptions: Assume flat tool list carries origin context; treat pseudo-code as exact SDK syntax; expect specifying server location to install every server automatically.
- UNVERIFIED: Causal step chains only if genuine, else 'none - omission beats invented process': configure and install multiple MCP servers -> connect -> discover/load their tools -> agent receives flat list (chunk9:206-290).

### 4. MCP resources versus tools
- Key concepts: MCP tools; MCP resources; prompt templates; tool calls for actions/API domains; resource schema; read-only data; reduced back-and-forth; local SQLite todo example; create/update/delete as tools; list as resource; Agent SDK custom server loading; allowed tools.
- UNVERIFIED: Exam-relevant facts: transcript says prompt templates are not useful in this treatment, while resources are exam-relevant; tools are functions commonly used for an API domain and can call database/SQL work; resources map data/schema for direct reading and are described as faster with less back-and-forth; working distinction is resources for read-only actions and tools for actions; presenter observes both are callable functions internally and does not claim resources enforce read-only behavior (chunk9:291-471).
- Candidate objectives (draft wording + ID ccaf-cNN-oM): `ccaf-cNN-o7` - Choose an MCP resource for described read-only data access and a tool for create, update, or delete action. `ccaf-cNN-o8` - Explain transcript-stated schema/direct-read rationale for resources without claiming stronger guarantees than source provides. `ccaf-cNN-o9` - Recognize that internal callable implementation does not erase resource-versus-action design intent.
- Glossary terms with g- slugs: `g-mcp-resource`, `g-mcp-tool`, `g-resource-schema`, `g-read-only`, `g-tool-call`, `g-sqlite`, `g-agent-sdk`, `g-allowed-tools`, `g-prompt-template`.
- UNVERIFIED: Diagram candidates with archetype from concept|process|comparison|decision|worked-scenario|callout|diagram|mcq-set|active-recall: `comparison` - resource: list todos/read data versus tool: create/update/delete todo; include shared callable-server implementation as bounded callout. `worked-scenario` - user asks list, then create, update, list, delete against todo server.
- UNVERIFIED: Comparison/decision table candidates with rows and columns: columns `Capability`, `Purpose`, `Read/write intent`, `Example`, `Transcript caveat`; rows `MCP resource`, `MCP tool`, `prompt template`.
- UNVERIFIED: MCQ candidates (count + applied stem ideas): 4. Choose capability for inventory lookup versus order update; identify benefit attributed to resource; reject claim resources cannot be callable functions; select correct todo operation mapping.
- UNVERIFIED: Recall prompts (count + prompts): 4. What distinction should learner use for resource versus tool? Which todo operations are tools in example? Which is resource? What caveat does presenter give about read-only enforcement?
- UNVERIFIED: Confusion pairs with discriminator: MCP resource versus MCP tool - resource is read-only data-access intent; tool performs action. Read-only design intent versus enforced immutability - transcript says one could put anything in resource, so no enforcement claim. Resource schema versus API endpoint - schema maps expected source data; endpoint returns data but can require tool-call flow.
- UNVERIFIED: Common wrong turn misconceptions: Treat resources as no-function direct database connection; put mutation behind resource and call it read-only; say prompt templates receive same exam emphasis as resources; rely on generated SDK code without inspecting fit.
- UNVERIFIED: Causal step chains only if genuine, else 'none - omission beats invented process': Agent SDK loads custom todo MCP server -> agent reads list through resource -> agent performs create/update/delete through tools -> agent can read list again (chunk9:395-460).

### 5. Claude Code with GitHub Actions
- UNVERIFIED: Key concepts: Claude Code action; GitHub workflow file; issue/PR/comment triggers; repository permissions; GitHub app installation; action secrets; separate API platform/key versus subscription assumption; workflow model and turn settings; action environment startup latency; issue feedback; branches; pull requests; merge conflicts; small files/workflow validation.
- UNVERIFIED: Exam-relevant facts: transcript describes GitHub Actions as triggering Claude Code from repository workflows and potentially creating PRs, implementing work, and following standards; triggers may include issue/PR comments, issue creation, assignment, and submission; action is slow because environment spins up as well as Claude executing; setup shown includes repo, GitHub app, repository access permissions, workflow file, and Anthropic API key stored in repository secrets; presenter infers API key is separate from subscription but does not establish it as universal fact; workflow can configure model, turns, and additional system prompt; actual demonstration exposed missing/unclear PR association, duplicate branches, conflict risk, and generated page error after manual merge (chunk9:472-1286).
- Candidate objectives (draft wording + ID ccaf-cNN-oM): `ccaf-cNN-o10` - Identify workflow trigger, permission, secret, and repository prerequisites shown for GitHub Actions-based Claude Code. `ccaf-cNN-o11` - Evaluate GitHub Actions invocation against interactive development when startup latency and asynchronous feedback matter. `ccaf-cNN-o12` - Diagnose why an issue comment, branch, and PR need explicit workflow verification rather than assumed association. `ccaf-cNN-o13` - Identify workflow-level controls named in transcript: model, turn count, additional system prompt.
- Glossary terms with g- slugs: `g-github-actions`, `g-claude-code-action`, `g-workflow`, `g-workflow-trigger`, `g-repository-secret`, `g-api-key`, `g-github-app`, `g-repository-permission`, `g-issue-comment`, `g-pull-request`, `g-branch`, `g-merge-conflict`.
- UNVERIFIED: Diagram candidates with archetype from concept|process|comparison|decision|worked-scenario|callout|diagram|mcq-set|active-recall: `process` - issue/PR event or mention -> workflow trigger -> runner/environment startup -> Claude Code action -> branch/changes -> PR/comment/review. `decision` - need low-latency conversation? yes -> action latency is stated drawback; no -> workflow automation may fit. `callout` - validate branch and PR association; do not assume it.
- UNVERIFIED: Comparison/decision table candidates with rows and columns: columns `Concern`, `Workflow/action behavior`, `Observed risk or tradeoff`, `Validation`; rows `trigger`, `permissions`, `API key/secret`, `startup time`, `branch/PR creation`, `manual merge`.
- UNVERIFIED: MCQ candidates (count + applied stem ideas): 5. Select prerequisite missing when action cannot authenticate; choose trigger to react to an issue comment; identify source of stated slowness; choose where API key is placed in demo; diagnose duplicated implementation/merge conflict after unclear branch-to-issue association.
- UNVERIFIED: Recall prompts (count + prompts): 5. Name trigger types mentioned. Why are GitHub Actions slow per transcript? What access categories were checked? What workflow controls can be adjusted? What failed assumption should be verified for branch and PR flow?
- UNVERIFIED: Confusion pairs with discriminator: GitHub app versus GitHub desktop/application - transcript clarifies GitHub app is repository integration. Subscription versus API key - demo treats platform API key/credits as separate setup, while exact product policy is not established. Issue mention versus automatic complete PR - transcript says capability is described but demo required setup and did not initially yield expected PR. Workflow run completion versus correct implementation - run can complete while output/merge remains wrong.
- UNVERIFIED: Common wrong turn misconceptions: Assume `@Claude` alone guarantees execution; assume all repo permissions are already sufficient; use secret in source instead of repository secret; equate action completion with correct PR; merge duplicate/conflicting outputs without inspection; infer subscription always covers action API usage from presenter uncertainty.
- Causal step chains only if genuine, else 'none - omission beats invented process': 1. Install/configure integration and repository permissions. 2. Add workflow and repository secret. 3. Trigger by configured issue/PR event or comment. 4. Runner starts and invokes action. 5. Inspect comment, changes, branch, and PR before merge (chunk9:472-1286).

### 6. Support-agent escalation
- Key concepts: support agent; Anthropic Agent SDK; mocked API-like MCP tool calls; interactive loop; support request categories; escalation state machine; session context; human escalation tool; explicit human request; legal threat; fraud claim/dispute; extreme frustration; repeated unsuccessful resolution; policy ambiguity; clarification for multiple matches; autonomous resolution where policy permits; sentiment/confidence limits.
- UNVERIFIED: Exam-relevant facts: example support scope includes refunds, course swaps, confirmation email, platform bugs, content issues, business development, and GDPR requests; presenter specifies Agent SDK rather than low-level SDK and mocks operations as tool calls/MCP server; initial generated code had unexpected tool-handler issue and was corrected to a tool loop; state labels shown are bot handling, escalation triggered, human queued, human active, resolved; stated escalation triggers include explicit request for human, legal threat, fraud claim/dispute, extreme frustration, failure after two or three attempts, and request failure; transcript says explicit human request should be honored immediately; when frustrated without explicit human request, acknowledge and offer resolution, escalating only when needed; policy ambiguity/out-of-scope/gray area should escalate rather than guess; multiple tool matches require additional information rather than guessing; source calls sentiment-based escalation and self-reported confidence unreliable proxies for actual use-case complexity, but presenter expresses uncertainty about exact rationale (chunk9:1287-1841).
- Candidate objectives (draft wording + ID ccaf-cNN-oM): `ccaf-cNN-o14` - Choose escalation for explicit human request, legal/fraud dispute, unresolved attempts, request failure, or policy ambiguity. `ccaf-cNN-o15` - Distinguish acknowledging frustration and attempting straightforward resolution from immediate escalation triggered by explicit human request. `ccaf-cNN-o16` - Select clarification over choosing among multiple matching tool records. `ccaf-cNN-o17` - Explain why sentiment and self-reported confidence must not be sole complexity proxies, retaining transcript uncertainty about detailed rationale. `ccaf-cNN-o18` - Map support-agent state and session context to progressive escalation workflow.
- Glossary terms with g- slugs: `g-support-agent`, `g-escalation`, `g-progressive-escalation`, `g-state-machine`, `g-session-context`, `g-human-queue`, `g-policy-ambiguity`, `g-explicit-human-request`, `g-sentiment-based-escalation`, `g-self-reported-confidence`, `g-clarification`, `g-gdpr-request`.
- Diagram candidates with archetype from concept|process|comparison|decision|worked-scenario|callout|diagram|mcq-set|active-recall: `decision` - request -> explicit human/legal/fraud/policy ambiguity/repeated failure? -> escalate; otherwise resolve or ask clarification. `process` - bot handling -> escalation triggered -> human queued -> human active -> resolved. `worked-scenario` - duplicate customer records -> ask identifier, do not select record; policy price-match question -> human escalation.
- Comparison/decision table candidates with rows and columns: columns `Situation`, `First response`, `Escalate now?`, `Reason/source boundary`; rows `explicit human request`, `frustrated customer without request`, `policy ambiguity`, `multiple records`, `straightforward refund/swap`, `legal/fraud dispute`, `two or three failed attempts`.
- UNVERIFIED: MCQ candidates (count + applied stem ideas): 6. Choose action when user immediately asks for person; choose response to angry but solvable refund; select route for competitor price match not covered by policy; choose action on two matching orders; identify escalation state after trigger; reject sole reliance on sentiment/confidence score for complexity.
- UNVERIFIED: Recall prompts (count + prompts): 6. List state labels in example. What triggers immediate human escalation? What should happen on policy gray area? What should agent do for multiple matching records? How are frustration and explicit human request handled differently? What does transcript say about sentiment/confidence proxies?
- Confusion pairs with discriminator: explicit human request versus frustration - explicit request escalates immediately; frustration alone receives acknowledgement and offered solution first. Policy ambiguity versus ordinary supported request - ambiguity escalates rather than improvising. Multiple matches versus one verified record - multiple matches need identifier/clarification; do not choose. Sentiment versus complexity - emotional tone is not reliable standalone complexity measure. Tool request failure versus agent ignorance - both appear in stated escalation triggers, but policy ambiguity explicitly forbids guessing.
- UNVERIFIED: Common wrong turn misconceptions: Force investigation before honoring explicit human request; use sentiment alone as escalation rule; improvise policy for price/merge/account-data cases; choose one of multiple records; make state machine only for escalation while leaving policy paths untested; assume generated agent code is correct without running tool loop and observing errors.
- Causal step chains only if genuine, else 'none - omission beats invented process': 1. Receive request and retain turn/reason/message state. 2. Determine explicit escalation trigger or supported resolution path. 3. If multiple records, ask for identifier; if ambiguity/trigger, invoke human-escalation tool. 4. Move bot handling -> escalation triggered -> human queued -> human active -> resolved (chunk9:1425-1535, 1595-1841).

## Proposed chapter mapping
| Proposed chapter | Lessons | Draft objective IDs | Rationale and static page shape |
|---|---|---|---|
| CCAF-NN: Agent architecture and operational patterns | 1, 2 | `ccaf-cNN-o1` to `ccaf-cNN-o4` | Place skill boundary and asynchronous batch operation together only as operational pattern coverage; preserve separate lesson sections and batch decision table. |
| UNVERIFIED: CCAF-NN: MCP server integration | 3, 4 | `ccaf-cNN-o5` to `ccaf-cNN-o9` | One MCP chapter: discovery first, then resources/tools discrimination; use flat-list concept and read/action comparison. |
| UNVERIFIED: CCAF-NN: Repository automation with Claude Code | 5 | `ccaf-cNN-o10` to `ccaf-cNN-o13` | Single implementation-governance chapter; separate documented facts from presenter observations and unresolved setup behavior. |
| CCAF-NN: Safe agent escalation | 6 | `ccaf-cNN-o14` to `ccaf-cNN-o18` | Single decision-heavy chapter with state diagram, escalation matrix, and applied support scenarios. |
- Keep `NN` unresolved until all chunk plans produce ordered chapter map and registry-first objective allocation. No current plan asserts exam blueprint domain weighting.

## Hands-on / lab content
- UNVERIFIED: Batch lab: static, non-executing walkthrough. Learner labels per-request `custom_id`, records hypothetical batch ID/state file, then decides submit versus check versus blocking run for urgency/cost scenarios. Do not call API or wait for batch result.
- MCP lab: paper/local-code pseudocode inspection only. Classify todo `list` as resource and create/update/delete as tools; map several server tool sets into an agent flat list; flag source-origin ambiguity. Do not install MCP server.
- GitHub Actions lab: offline workflow-review worksheet. Given trigger, permissions, secret location, action latency, branch, and PR checklist, identify missing prerequisite or validation. Do not authenticate, install app, create secret, invoke cloud action, or create repo event.
- Escalation lab: fill a support decision/state matrix for immediate human request, frustration, ambiguity, duplicate match, fraud/legal issue, and ordinary refund. Write explicit state transitions and expected tool/clarification choice. Do not build agent, use API, or process personal/customer data.
- Validation after later authoring: static page contracts and `tools/verify.ps1`; this plan itself changes no runtime content.

## Open questions and gaps
- Whole-topic synthesis must assign real chapter numbers in `ccaf-cNN-oM`; all IDs are drafts.
- Transcript calls resources exam-relevant but gives no authoritative MCP specification, exact resource schema, or enforcement semantics. Teach only read-only intent and stated caveat.
- Transcript says prompt templates are not useful; this is presenter perspective, not a general platform claim.
- UNVERIFIED: Batch statement says up to 24 hours and no guaranteed SLA in this course context; later source verification may be needed before presenting as current product policy.
- UNVERIFIED: GitHub Actions setup/result is demonstrative and includes uncertainty: API-key/subscription relationship, automatic PR behavior, branch association, and action configuration must not become unqualified exam facts without authoritative material.
- GitHub integration section contains actual-secret exposure in source narrative; authored learning content must never reproduce keys, screenshots, or secret-like values.
- Support example uses simulated company policies, mock tools, and generated code; use it only for escalation patterns, not as product or legal/GDPR policy advice.
- Source explicitly leaves detailed explanation of sentiment/confidence unreliability unclear. Preserve as a bounded exam cue; do not invent causal theory.
- Need global deduplication against chunks 1-9 for MCP, tool use, Agent SDK, GitHub, and escalation topics before authoring final chapter order.

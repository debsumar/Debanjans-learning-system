## Chunk metadata

- UNVERIFIED: Topic: Claude Certified Architect - Foundations; slug `ccaf`; exam code `CCA-F`.
- Source: Andrew Brown course transcript, `/tmp/ccaf-chunks/chunk5`; global transcript lines 9206-11046; 1,841 source lines read in four 500-line-or-EOF slices.
- Scope: structured research findings and provenance; Agent SDK session forking; Claude Code sessions, resume, context, compaction, clearing, naming, rewinding; settings scopes and selected settings categories.
- UNVERIFIED: Planning boundary: source-derived draft only. No assertion here establishes current product behavior beyond instructor statements/demos. Verify version-sensitive SDK, command, retention, and setting details against official material before authoring.
- Proposed objective IDs are drafts. Chapter number `NN` remains TBD until full-topic chapter map and registry exist.

## Lesson inventory

| # | title | chunk line refs | one-line summary |
|:--|:--|:--|:--|
| 1 | Findings provenance and structured output | 9206-9705 | Raw claims lose attribution; preserve claim data and source metadata together in structured findings. |
| 2 | Fork-based session management | 9706-10205 | Branch one shared conversational point into isolated session histories for parallel exploration. |
| 3 | Resume and fork verification lab | 10120-10320 | Test resume and fork behavior rather than trusting generated code or assumed SDK parity. |
| UNVERIFIED: 4 | Claude Code sessions and resume | 10206-10435 | A session is a conversation; resume/select prior sessions with local and web origin considerations. |
| UNVERIFIED: 5 | Forking a Claude Code session | 10400-10510 | Fork creates a new conversation/session ID while preserving original conversation. |
| 6 | Context inspection | 10480-10665 | Context reports current-session token use by category and reserves autocompaction headroom. |
| 7 | Compact and clear | 10620-10805 | Compact reduces context through summary; clear removes conversation history and starts fresh. |
| 8 | Rename and rewind | 10770-10890 | Rename improves session identification; rewind restores or summarizes from an older conversation point. |
| 9 | Settings scopes | 10880-10970 | Managed, user, project, and local settings differ by ownership, location, Git treatment, and precedence. |
| 10 | Settings categories overview | 10950-11046 | Review authentication, session/storage, environment, model, and output/language setting categories. |

## Per-lesson plan

### 1. Findings provenance and structured output

- Key concepts: provenance; raw findings dilemma; claim attribution; structured findings; source metadata; validated JSON; persistence to results file; inspect generated code instead of accepting it.
- UNVERIFIED: Exam-relevant facts: Raw outputs can retain claims while losing source, document, and page attribution; preserve content plus metadata in a structured form (9206-9230). Demo seeks structured/validated findings, an agent definition, and saved results rather than prompt-only JSON instructions (9320-9550). Generated code changed intended details during revision; instructor explicitly says review and verify rather than vibe-code (9410-9600). Findings example exposes content, confidence, claim/type, and source-related fields, but instructor identifies missing origin detail as a gap (9640-9705).
- Candidate objectives: `ccaf-cNN-o1` (draft: Preserve claim provenance by carrying source metadata with each research finding); `ccaf-cNN-o2` (draft: Evaluate generated agent code for schema validation, agent definition, output persistence, and use-case preservation).
- Glossary terms with g- slugs: provenance `g-provenance`; finding `g-finding`; structured output `g-structured-output`; schema validation `g-schema-validation`; agent definition `g-agent-definition`; research state `g-research-state`; confidence `g-confidence`; output persistence `g-output-persistence`.
- Diagram candidates: `concept` - finding record split into claim, confidence, source/document/page, and persisted result; `process` - research request -> structured validation -> review -> save findings -> inspect provenance; `callout` - raw claim without attribution versus claim carrying source metadata.
- Comparison/decision table candidates with rows and columns: Table `Raw findings versus structured findings`; rows: claim text, confidence, source/document, page/location, validation, persistence, auditability; columns: raw output, structured finding, author implication. Table `Generated implementation review`; rows: requested use case, SDK choice, agent definition, schema enforcement, save behavior; columns: requested, generated, reviewer check.
- UNVERIFIED: MCQ candidates: 3. Applied stems: choose data needed to support a sourced market claim after summary; identify missing field when a finding has claim/confidence but cannot trace origin; select review action after generated revision changes requested research scope.
- UNVERIFIED: Recall prompts: 4. Define provenance in a finding; list metadata needed to trace a claim; explain why prompt text alone is weak schema enforcement; name two implementation properties to inspect before running generated research code.
- Confusion pairs with discriminator: provenance vs confidence - provenance identifies origin, confidence expresses assessed certainty; structured output request vs validated structured output - request asks model for shape, validation enforces/assesses shape; stored findings vs displayed findings - storage supports later retrieval/audit, display alone may not.
- Common wrong turn misconceptions: "A plausible final summary retains its citations automatically"; "JSON-only wording guarantees valid structured data"; "Changing SDK references cannot alter research requirements"; "If output prints, it is persistently available."
- Causal step chains only if genuine: Source claim is summarized without attached attribution -> final output cannot identify document/page/source -> preserve content plus metadata in each structured finding (9206-9230). Generated implementation is revised without careful review -> requested detail can be lost or altered -> inspect against original use case before execution (9410-9600).

### 2. Fork-based session management

- Key concepts: fork-based session management; baseline/common history; branch; isolated state; session history; session ID; concurrent/parallel branches; Agent SDK; TypeScript and Python capability divergence.
- UNVERIFIED: Exam-relevant facts: Forking branches one conversation point into separate session histories, each with its own ID; branches are isolated and do not share knowledge (9706-9765). Instructor says the Agent SDK `forkSession` capability shown is TypeScript-only at that time, not Python (9706-9785, 9920-9985). Demo baseline analyzes a market, then branches optimistic, pessimistic, and regulation questions; later TypeScript run forks three isolated branches concurrently (9800-10170). Parallel work returns faster but instructor notes greater credit consumption (10150-10205). Course repeatedly distinguishes generated example code from verified behavior (9760-9795, 9910-9935).
- Candidate objectives: `ccaf-cNN-o3` (draft: Select session forking when independent analyses must share a common prior conversation without sharing later branch state); `ccaf-cNN-o4` (draft: Identify baseline, branch isolation, resume identity, concurrency benefit, and consumption trade-off in a forked-session design); `ccaf-cNN-o5` (draft: Verify SDK language capability before choosing a session-fork implementation).
- UNVERIFIED: Glossary terms with g- slugs: fork-based session management `g-fork-based-session-management`; baseline `g-session-baseline`; branch `g-session-branch`; isolated state `g-isolated-session-state`; session ID `g-session-id`; session history `g-session-history`; `forkSession` `g-fork-session`; concurrency `g-concurrency`.
- Diagram candidates: `process` - shared baseline -> fork A/B/C -> isolated histories -> independent results; `concept` - original session versus branch session IDs and state boundaries; `decision` - need independent continuation from same prior context? -> fork; need one continuing conversation? -> resume/continue; `worked-scenario` - market baseline then optimistic/pessimistic/regulatory branch questions.
- UNVERIFIED: Comparison/decision table candidates with rows and columns: Table `Shared baseline branches`; rows: prior history, session ID, later messages, visibility of sibling work, execution timing, result latency, consumption; columns: original, branch A, branch B, branch C. Table `Session design choice`; rows: preserve source conversation, independent follow-up, common starting context, language support, cost/speed; columns: continue one session, fork sessions, manually duplicated prompts.
- UNVERIFIED: MCQ candidates: 4. Applied stems: choose architecture for three independent analyses of one completed market summary; identify false claim about branch knowledge sharing; select language/API investigation after a Python example lacks durable session behavior; assess speed/cost consequence of concurrently run branches.
- UNVERIFIED: Recall prompts: 5. What does a fork inherit? What remains isolated after a fork? Why does each branch need its own session ID? State baseline-to-branch flow. What version/language question must be checked before implementation?
- Confusion pairs with discriminator: fork vs resume - fork creates new divergent conversation from prior point, resume continues prior conversation; parallel branches vs shared-state collaboration - branches run independently and do not share later knowledge; manually repeated prompts vs session forking - repeated prompts recreate context externally, fork preserves a session history/identity per course framing; SDK API availability vs generated code suggestion - capability must be verified in target language/version.
- Common wrong turn misconceptions: "Three prompts from same text are automatically three managed session branches"; "Forked branches learn one another's later results"; "Python and TypeScript SDKs expose identical session APIs"; "Parallel output is free because it returns sooner."
- UNVERIFIED: Causal step chains only if genuine: Build common baseline -> fork at that point -> assign each branch a new session history/ID -> run independent follow-ups concurrently -> receive isolated results faster, with more credit consumption noted (9800-10205).

### 3. Resume and fork verification lab

- Key concepts: empirical verification; session initiation; session resumption by ID; forked conversation; persisted session artifacts; workspace scoping; generated test input; reproducible test.
- UNVERIFIED: Exam-relevant facts: Lab creates a first session, resumes it by session ID, then forks a conversation and observes separate sessions/artifacts (10120-10320). Instructor deletes observed files and reruns to test where session information is written, while noting uncertainty around active-session behavior (10220-10310). The lab framing requires testing whether resume/fork exists in Agent SDK rather than assuming Claude Code behavior transfers (10120-10205).
- Candidate objectives: `ccaf-cNN-o6` (draft: Design a minimal test that distinguishes new-session, resumed-session, and forked-session behavior); `ccaf-cNN-o7` (draft: Verify session persistence/location claims by controlled rerun rather than inference).
- Glossary terms with g- slugs: session resume `g-session-resume`; session artifact `g-session-artifact`; controlled rerun `g-controlled-rerun`; workspace scope `g-workspace-scope`; reproducible test `g-reproducible-test`.
- Diagram candidates: `worked-scenario` - session 1 reads test file -> resume with ID -> fork -> inspect resulting session artifacts; `process` - observe -> remove non-active artifacts -> rerun -> compare artifacts -> conclude only what evidence supports; `callout` - "generated code says feature exists" versus "run verifies feature behavior".
- Comparison/decision table candidates with rows and columns: Table `Session test outcomes`; rows: initial query, resumed query, forked query, original history affected, new ID expected, artifact observation; columns: new session, resume, fork. Table `Evidence quality`; rows: generated assertion, UI appearance, command result, persisted artifact, controlled rerun; columns: evidence, what it supports, remaining uncertainty.
- UNVERIFIED: MCQ candidates: 3. Applied stems: choose test proving a branch did not overwrite original conversation; identify weakest evidence for SDK capability; choose next action when observed session files include an active-session exception.
- UNVERIFIED: Recall prompts: 3. List three operations to test separately. Why remove/rerun artifacts? What must a test observe before claiming persistence location?
- Confusion pairs with discriminator: observation vs conclusion - observed files support an artifact claim, not undocumented storage guarantees; session ID resume vs a new query - resume supplies/chooses prior session identity; test fixture vs production content - deliberate buggy input exists to exercise behavior, not to establish application design.
- UNVERIFIED: Common wrong turn misconceptions: "One successful output proves session storage contract"; "Deleting files while session active proves they are unnecessary"; "A Claude Code feature automatically proves Agent SDK support."
- Causal step chains only if genuine: Start first query -> record/use session ID to resume prior conversation -> fork to create distinct conversation -> inspect behavior/artifacts; controlled deletion and rerun isolate whether artifacts are regenerated (10120-10320).

### 4. Claude Code sessions and resume

- Key concepts: Claude Code session; conversation; web session; local session; remote control/web origin; workspace; resume command; conversation selection; session listing UI; project scope.
- UNVERIFIED: Exam-relevant facts: Course defines a Claude Code session functionally as a conversation; typing `claude` starts a new session (10206-10245). It describes web and local sessions as origin-dependent; demo reports VS Code can resume a web conversation locally (10220-10270). Resume chooses a prior session; terminated terminal session shows resume information including a session ID, but instructor does not verify a non-interactive ID-list command (10255-10305). Demo warns session may need original workspace/folder; opening in wrong directory yields missing-file/settings issues (10310-10380). CLI resume list has project display/toggle/preview/rename controls in demo (10375-10435).
- Candidate objectives: `ccaf-cNN-o8` (draft: Explain session as a conversation and select resume when continuation needs prior conversation state); `ccaf-cNN-o9` (draft: Diagnose workspace mismatch when resuming a prior session); `ccaf-cNN-o10` (draft: Distinguish source origin labels from project/session scope in course examples).
- Glossary terms with g- slugs: Claude Code session `g-claude-code-session`; web session `g-web-session`; local session `g-local-session`; workspace `g-workspace`; resume command `g-resume-command`; session origin `g-session-origin`; project scope `g-project-scope`.
- UNVERIFIED: Diagram candidates: `concept` - new `claude` invocation -> new session; resume selection -> prior session continuation; `decision` - prior task needs its history? -> resume in matching workspace; no prior history needed? -> new session; `worked-scenario` - web-origin conversation selected locally, then prompt to open original folder.
- Comparison/decision table candidates with rows and columns: Table `Session access modes`; rows: origin, displayed scope, workspace requirement, continuation method, uncertainty stated by instructor; columns: web, local. Table `New versus resumed session`; rows: history available, session identity, workspace sensitivity, use case; columns: new, resumed.
- UNVERIFIED: MCQ candidates: 4. Applied stems: select response when a resumed session reports files/settings from another project; decide whether to start new or resume for continuation task; identify what a web/local label establishes; choose evidence-supported statement about programmatic ID enumeration.
- UNVERIFIED: Recall prompts: 4. Define session in course terms. What does typing `claude` do? Why can workspace matter on resume? What uncertainty did instructor leave about non-interactive session-ID listing?
- Confusion pairs with discriminator: session vs project - session is a conversation; project/workspace supplies repository context; web origin vs local origin - indicates start location, not necessarily capability equivalence; resume selector vs fork - selector continues listed history, fork creates divergent history.
- Common wrong turn misconceptions: "Any session can safely resume in any folder"; "Web/local labels mean one cannot continue from the other"; "Course demo confirms a stable API for listing IDs non-interactively."
- Causal step chains only if genuine: Start session in a workspace -> session carries conversation/repository context -> attempt continuation elsewhere -> missing files/settings can result -> open original folder or continue in matching workspace (10220-10380).

### 5. Forking a Claude Code session

- Key concepts: fork command; original session; forked session; divergence; separate conversation; session ID; branch visibility; session naming.
- UNVERIFIED: Exam-relevant facts: Forking resumes/branches from prior session without affecting original and creates a new conversation with its own session ID (10400-10435). Demo starts a coding conversation, invokes fork, asks separate bug-check question in fork, and observes original plus forked conversations (10420-10510). Instructor notes renaming can help distinguish forks (10485-10510).
- Candidate objectives: `ccaf-cNN-o11` (draft: Use session fork to explore a follow-up without changing original conversation); `ccaf-cNN-o12` (draft: Identify evidence that original and forked sessions are distinct conversations).
- Glossary terms with g- slugs: fork command `g-fork-command`; original session `g-original-session`; forked session `g-forked-session`; divergence `g-session-divergence`; session rename `g-session-rename`.
- UNVERIFIED: Diagram candidates: `process` - original coding conversation -> fork command -> original unchanged plus bug-review conversation; `comparison` - resume original versus fork original; `callout` - new fork ID and separate conversation label.
- Comparison/decision table candidates with rows and columns: Table `Continue versus fork`; rows: original history changed, new session ID, independent direction, original retained, typical prompt; columns: resume/continue, fork. Table `Conversation labels`; rows: original task, fork purpose, rename value, risk of ambiguity; columns: unnamed sessions, named sessions.
- UNVERIFIED: MCQ candidates: 3. Applied stems: preserve working implementation discussion while testing a separate bug review; identify expected original-session effect after fork; choose organizational practice for several related branches.
- UNVERIFIED: Recall prompts: 3. What does fork protect? What new identifier/conversation does it create? Why rename a branch?
- Confusion pairs with discriminator: fork vs rewind - fork creates a distinct branch, rewind changes/restores current conversation point; fork vs resume - fork preserves original while divergence begins, resume continues original; session label vs session ID - label aids people, ID identifies session.
- Common wrong turn misconceptions: "Fork edits/deletes prior session history"; "A fork is only a renamed original"; "A separate prompt in original session is equivalent to a fork."
- Causal step chains only if genuine: Establish original conversation -> issue fork -> system creates distinct conversation/ID from prior point -> send alternate follow-up in branch while original remains separate (10400-10510).

### 6. Context inspection

- Key concepts: context command; token consumption; available tokens; category breakdown; system prompt; skills; messages; autocompact buffer; current session; session switching.
- UNVERIFIED: Exam-relevant facts: Context command shows consumed and available tokens for current session, broken down by category (10480-10535). New session has nearly all space available except loaded material and autocompact buffer; course states buffer reserves 22% of context window for history summarization headroom (10495-10530). Older session shows system prompt, skills, and message usage; selecting another session changes displayed context information (10520-10665). Instructor describes context check as useful after resuming a prior session (10520-10545).
- Candidate objectives: `ccaf-cNN-o13` (draft: Interpret current-session context usage by category and use it to decide whether to manage context); `ccaf-cNN-o14` (draft: Explain autocompact buffer purpose as stated in course); `ccaf-cNN-o15` (draft: Verify context data after changing active session rather than assuming it is global).
- Glossary terms with g- slugs: context command `g-context-command`; context window `g-context-window`; token consumption `g-token-consumption`; available tokens `g-available-tokens`; autocompact buffer `g-autocompact-buffer`; system prompt `g-system-prompt`; loaded skill `g-loaded-skill`.
- Diagram candidates: `concept` - context window segmented into system prompt, skills, messages, autocompact buffer, available space; `process` - resume/select session -> run context -> inspect category usage -> choose compact/clear or continue; `comparison` - new session context versus older resumed session context.
- UNVERIFIED: Comparison/decision table candidates with rows and columns: Table `Context category reading`; rows: system prompt, skills, messages, autocompact buffer, available capacity; columns: what command reports, operational implication. Table `Session context`; rows: active history, category totals, need for action; columns: new session, resumed session A, resumed session B.
- UNVERIFIED: MCQ candidates: 3. Applied stems: identify why context display changed after selecting a session; choose correct meaning of autocompact buffer; decide which command to inspect before a long continuation of resumed work.
- UNVERIFIED: Recall prompts: 4. What does context report? Name three reported categories. What is autocompact buffer for? Why re-run context after session switch?
- UNVERIFIED: Confusion pairs with discriminator: context usage vs account/subscription usage - context is current conversation window measure; available tokens vs autocompact buffer - available is usable remaining space, buffer is reserved headroom; new session vs low usage within old session - session identity controls display.
- Common wrong turn misconceptions: "Context percent applies to every saved session"; "Autocompact buffer is ordinary free capacity"; "Switching conversations leaves context inspection unchanged."
- Causal step chains only if genuine: Select/resume session -> run context -> category usage reflects that active session -> compare available space/headroom -> decide whether further context management is warranted (10480-10665).

### 7. Compact and clear

- UNVERIFIED: Key concepts: automatic compaction; manual compact; clear; conversation history; context recovery; cost/subscription usage; retained project instructions; automation memory; terminal command collision; environment interruption.
- UNVERIFIED: Exam-relevant facts: Larger conversations consume more tokens and can increase API cost or use subscription window sooner; course says Claude Code autocompacts at larger sizes, with manual compact/clear available (10620-10715). Demo shows compacted conversation substantially reduces context; clear then frees it to near-minimal state (10700-10765). Instructor says clear does not remove `CLAUDE.md` files or automemory, but clears conversation history and starts over (10670-10725). It warns terminal users not to use `clear` expecting only screen clearing; it also notes environment halt experience and recommends monitoring context (10690-10735).
- Candidate objectives: `ccaf-cNN-o16` (draft: Select compact versus clear based on whether summarized continuity or a fresh conversation is required); `ccaf-cNN-o17` (draft: Identify what clear removes and does not remove according to course); `ccaf-cNN-o18` (draft: Monitor context before it becomes operationally constraining).
- UNVERIFIED: Glossary terms with g- slugs: compaction `g-context-compaction`; manual compact `g-manual-compact`; clear command `g-clear-command`; conversation history `g-conversation-history`; automemory `g-automemory`; `CLAUDE.md` `g-claude-md`; context recovery `g-context-recovery`.
- UNVERIFIED: Diagram candidates: `process` - growing conversation -> inspect context -> compact to summarize/reduce -> inspect again; separate branch -> clear -> new conversation state; `decision` - need preserved summarized continuity? -> compact; need no prior conversation history? -> clear; `callout` - command `clear` is destructive to conversation history, not merely terminal display.
- UNVERIFIED: Comparison/decision table candidates with rows and columns: Table `Compact versus clear`; rows: prior history retained, summary created, context reduction, `CLAUDE.md`/automemory impact, reversibility risk, use case; columns: compact, clear. Table `Context pressure signals`; rows: message growth, cost/subscription concern, available room, environment instability observation; columns: observed signal, inspect/action.
- UNVERIFIED: MCQ candidates: 4. Applied stems: preserve task continuity but reduce context; start unrelated work without conversation history; identify asset unaffected by clear per course; avoid destructive command confusion in terminal.
- UNVERIFIED: Recall prompts: 4. Why can long conversations cost more? What does compact do? What does clear remove? Name two things clear does not remove according to source.
- UNVERIFIED: Confusion pairs with discriminator: compact vs clear - compact summarizes/reduces, clear deletes current conversation history; clear conversation vs clear terminal screen - same word, radically different effect in Claude Code context; automatic compaction vs manual compact - automatic occurs at larger sizes, manual is user-triggered.
- UNVERIFIED: Common wrong turn misconceptions: "Clear only clears terminal output"; "Clear deletes `CLAUDE.md` and automemory"; "Compaction leaves context size unchanged"; "Autocompaction means context never needs inspection."
- UNVERIFIED: Causal step chains only if genuine: Conversation grows -> token/context pressure increases -> inspect context -> compact summarizes and lowers usage; alternatively clear removes history and frees context while course-stated project instructions/automemory remain (10620-10765).

### 8. Rename and rewind

- UNVERIFIED: Key concepts: session rename; session discoverability; session retention observation; rewind; restore point; summarize from point; destructive history loss; token count change.
- UNVERIFIED: Exam-relevant facts: Rename changes current session name and is presented as a way to distinguish conversations (10770-10835). Instructor says sessions are available for 30 days, but separately flags backup behavior as an uncertainty to investigate (10770-10795); retain only as instructor-stated/version-sensitive fact. Rewind restores a session to an older point; demo offers restore conversation or summarize from chosen point and shows smaller context after restore (10790-10890). Instructor warns not to rewind too far if later material cannot be lost (10790-10815).
- UNVERIFIED: Candidate objectives: `ccaf-cNN-o19` (draft: Apply session names to improve later selection among similar conversations); `ccaf-cNN-o20` (draft: Differentiate rewind restore from summarize-from-point and identify history-loss risk); `ccaf-cNN-o21` (draft: Treat retention and backup behavior as version-sensitive operational facts requiring verification).
- UNVERIFIED: Glossary terms with g- slugs: rename command `g-rename-command`; rewind command `g-rewind-command`; restore point `g-restore-point`; summarize from point `g-summarize-from-point`; session retention `g-session-retention`; history loss `g-history-loss`.
- Diagram candidates: `decision` - need session identification? -> rename; need revisit old point? -> choose restore or summarize after checking loss risk; `process` - session timeline -> select prior prompt -> restore/summarize -> continue from selected point; `callout` - rewind can discard later context, verify target before confirming.
- UNVERIFIED: Comparison/decision table candidates with rows and columns: Table `Rewind options`; rows: later history, resulting context, continuation basis, loss risk, suitable goal; columns: restore conversation, summarize from point. Table `Session organization`; rows: human identification, retention uncertainty, backup consideration, branch differentiation; columns: unnamed, renamed.
- UNVERIFIED: MCQ candidates: 3. Applied stems: select safe option after deciding later details must remain accessible; identify purpose of rename amid related sessions; classify retention statement needing current-doc verification.
- UNVERIFIED: Recall prompts: 3. What does rename change? What two rewind choices appear in demo? Why inspect selected rewind point before confirmation?
- UNVERIFIED: Confusion pairs with discriminator: rewind vs fork - rewind changes current history position, fork preserves original and creates another conversation; restore vs summarize - restore returns conversation state, summarize reduces it from selected point; retention duration statement vs guaranteed archival policy - transcript observation is not verified retention contract.
- Common wrong turn misconceptions: "Rename changes underlying task/session content"; "Rewind always preserves every later exchange"; "30-day mention establishes permanent backup behavior."
- Causal step chains only if genuine: Choose earlier conversation point -> choose restore or summarize -> later conversation material can be removed/condensed -> context count can decrease -> continue from resulting state (10790-10890).

### 9. Settings scopes

- Key concepts: managed settings; user settings; project settings; local settings; organization ownership; Git inclusion; file location; precedence; managed-only settings.
- UNVERIFIED: Exam-relevant facts: Course orders scopes as managed, user, project, local; managed is organization-wide and IT/DevOps-managed, user is personal across projects, project is repository-specific, and local is project-specific but not checked into Git (10880-10950). Managed settings live on servers in managed settings file; user settings live under home directory; project/local are project/repository-oriented and local is distinguished by local JSON naming in demo (10905-10955). Higher scope takes precedence over lower scope; some settings work only in managed settings file (10905-10970).
- Candidate objectives: `ccaf-cNN-o22` (draft: Select settings scope based on audience, repository sharing, and organization control); `ccaf-cNN-o23` (draft: Resolve a settings conflict using course-stated precedence); `ccaf-cNN-o24` (draft: Identify why local project settings should not be assumed checked into Git).
- Glossary terms with g- slugs: managed settings `g-managed-settings`; user settings `g-user-settings`; project settings `g-project-settings`; local settings `g-local-settings`; settings precedence `g-settings-precedence`; Git-tracked settings `g-git-tracked-settings`; managed-only setting `g-managed-only-setting`.
- Diagram candidates: `concept` - four stacked scopes with higher-over-lower precedence; `decision` - organization-wide control -> managed; personal cross-project preference -> user; shareable repository convention -> project; machine/project-private value -> local; `comparison` - setting location and Git treatment across scopes.
- Comparison/decision table candidates with rows and columns: Table `Settings scope matrix`; rows: owner, intended reach, location described, Git treatment, priority, managed-only compatibility; columns: managed, user, project, local. Table `Scope selection`; rows: company policy, personal default, shared repo convention, local secret/private preference; columns: recommended scope, reason, source caveat.
- UNVERIFIED: MCQ candidates: 4. Applied stems: choose scope for IT-enforced organization instruction; choose scope for personal preference across repositories; identify why local setting did not appear in Git change; resolve conflicting managed and project values.
- UNVERIFIED: Recall prompts: 5. List settings scopes highest to lowest. Who manages each? Which is not checked into Git? Which scope wins conflict? What special limitation can apply to managed settings?
- Confusion pairs with discriminator: project vs local - both project-related, but local is not committed per course; managed vs user - organization/IT control versus individual preference; file location vs precedence - location describes storage, scope hierarchy decides conflict winner.
- Common wrong turn misconceptions: "Repository-specific always means committed"; "User setting outranks organization policy"; "Every setting is legal at every scope."
- Causal step chains only if genuine: Same setting exists at multiple scopes -> higher scope takes precedence -> effective behavior follows managed/user/project/local hierarchy (10880-10970).

### 10. Settings categories overview

- Key concepts: settings JSON; authentication settings; API key helper; temporary API key; AWS SSO refresh; login method; credential export; session cleanup period; persistence disablement; automemory directory; plans directory; environment variables; model default/restriction/mapping; thinking; fast mode; output style.
- UNVERIFIED: Exam-relevant facts: Transcript introduces authentication settings including custom API key helper, AWS SSO refresh/login-related options, login-method selection, and native credential export script output (10950-11005). Session/storage settings include cleanup period default of 30 days in course, configurable automemory/plans directories, and special behavior when cleanup period is zero: delete transactions at startup, disable persistence, write no new JSON, show nothing on resume, and give hooks empty transcript paths (10985-11035). Course says automemory directory cannot be in project Claude settings JSON (11005-11020). Environment variables can apply across sessions; model settings can override default model, restrict selection, map IDs to provider IDs, enable thinking by default, and configure fast mode behavior; output style changes explanatory behavior (11020-11046). Instructor describes fast mode as session opt-in/reset and gives a tentative Opus/cost framing; keep this version-sensitive.
- Candidate objectives: `ccaf-cNN-o25` (draft: Match a setting category to authentication, persistence, environment, model, or output behavior); `ccaf-cNN-o26` (draft: Predict course-stated consequences of session cleanup period zero); `ccaf-cNN-o27` (draft: Identify settings that need scope/location verification before configuring); `ccaf-cNN-o28` (draft: Distinguish model-selection controls from output-style controls).
- Glossary terms with g- slugs: settings JSON `g-settings-json`; API key helper `g-api-key-helper`; temporary API key `g-temporary-api-key`; AWS SSO refresh `g-aws-sso-refresh`; cleanup period `g-session-cleanup-period`; persistence disablement `g-persistence-disablement`; plans directory `g-plans-directory`; environment variable `g-session-environment-variable`; model mapping `g-model-mapping`; output style `g-output-style`.
- UNVERIFIED: Diagram candidates: `concept` - settings JSON categories: authentication, storage, environment, model, output/language; `process` - cleanup period set to zero -> startup deletion -> no new persisted JSON -> resume empty -> hooks empty transcript path; `decision` - desired behavior -> choose category, then verify legal scope and version-specific setting semantics; `callout` - zero cleanup is persistence-disabling behavior, not an ordinary retention adjustment.
- UNVERIFIED: Comparison/decision table candidates with rows and columns: Table `Settings category map`; rows: API key helper, SSO refresh, cleanup period, plans directory, environment variable, default model, output style; columns: category, stated effect, scope/location caveat, verification need. Table `Cleanup period outcomes`; rows: inactive-session deletion, startup transaction deletion, new JSON writes, resume listing, hook transcript path; columns: ordinary nonzero setting, zero setting as transcript states. Table `Model versus output settings`; rows: default model, allowed models, provider mapping, thinking default, fast mode, explanatory output; columns: model settings, output/language settings.
- UNVERIFIED: MCQ candidates: 5. Applied stems: identify consequence of cleanup period zero; choose category for temporary API-key generation script; choose setting category for cross-session environment variable; distinguish default-model restriction from explanatory output style; identify configuration needing project-scope restriction review.
- UNVERIFIED: Recall prompts: 5. Name five settings categories covered. List four stated effects of cleanup period zero. What does API key helper do? Which setting controls output style? Which category controls default/allowed model selection?
- UNVERIFIED: Confusion pairs with discriminator: cleanup period vs session retention backup strategy - cleanup controls deletion/persistence behavior, backup is separate operational concern; model default vs output style - model setting selects/limits model behavior, output style changes response presentation; environment variable vs API key helper - environment supplies values to sessions, helper generates temporary key; automemory directory vs plans directory - separate configurable storage targets with stated scope restriction for automemory.
- UNVERIFIED: Common wrong turn misconceptions: "Zero cleanup only shortens retention"; "Resume remains useful after persistence is disabled"; "All directory settings are legal in project settings"; "Output style selects model"; "Tentative instructor commentary establishes permanent fast-mode policy."
- UNVERIFIED: Causal step chains only if genuine: Set cleanup period to zero -> startup deletes transactions -> persistence disabled -> no new JSON session files -> resume lists nothing -> hooks receive empty transcript paths (10985-11035).

## Proposed chapter mapping

- Candidate chapter: `NN TBD - Research evidence and session lifecycle`; source lessons 1-3. Draft scope: traceable research findings; structured/validated outputs; independent-agent/session branches; verification-first implementation. Proposed objectives: `ccaf-cNN-o1` through `ccaf-cNN-o7`.
- Candidate chapter: `NN TBD - Claude Code session operations`; source lessons 4-8. Draft scope: new/resumed/forked sessions; workspace awareness; context inspection; compaction/clear; session naming and rewind safety. Proposed objectives: `ccaf-cNN-o8` through `ccaf-cNN-o21`.
- Candidate chapter: `NN TBD - Configuration governance`; source lessons 9-10. Draft scope: scope ownership/precedence; Git implications; authentication, persistence, environment, model, and output settings. Proposed objectives: `ccaf-cNN-o22` through `ccaf-cNN-o28`.
- UNVERIFIED: Cross-chapter confusion index: provenance versus confidence; fork versus resume versus rewind; context window versus subscription/account usage; compact versus clear; managed/user/project/local scope; cleanup retention versus backup/persistence.
- Do not assign final chapter numbers, exam weighting, service names, command syntax, or current SDK language support until certification blueprint and official versioned documentation are available.

## Hands-on / lab content

- Lab A - Provenance-preserving findings: Given three source claims, define record fields for claim, confidence, source/document, page/location, and persistence target. Learner review checklist compares requested use case, schema validation, agent definition, and saved output. Static lesson only; no live API key, model call, or remote source required. Source basis: 9206-9705.
- Lab B - Fork design on paper: Start from one baseline market analysis, write three independent follow-up questions, assign branch names/IDs, state isolation boundary, and predict speed/credit trade-off. Do not claim runtime outcome without current SDK verification. Source basis: 9706-10205.
- Lab C - Session lifecycle test protocol: Specify observation table for new, resumed, and forked session; include original-history preservation, identity, artifact observation, workspace, and controlled rerun columns. Learner records evidence and uncertainty instead of deleting local session data. Source basis: 10120-10435.
- UNVERIFIED: Lab D - Context decision drill: Given context breakdowns for new and old sessions, select continue, compact, or clear and justify based on preservation requirement. Include warning that `clear` is not superficial terminal screen clearing. Source basis: 10480-10765.
- Lab E - Scope selection matrix: Map organization policy, personal default, shared project convention, and private project-local preference to managed/user/project/local scope. Resolve two conflicts using higher-scope precedence. Source basis: 10880-10970.
- Lab F - Cleanup-zero impact trace: Complete cause/effect chain for zero cleanup setting using only transcript-stated outcomes. Mark settings/version facts to confirm with current official docs before practical configuration. Source basis: 10985-11035.
- No executable labs, credentials, package installation, SDK code, shell commands, repository changes, or topic files belong in this planning chunk.

## Open questions and gaps

- Course transcript is not certification blueprint. Need official CCA-F objective map, exam weights, terminology, supported product versions, and expected depth before final objective IDs/chapter numbers.
- UNVERIFIED: Confirm current Agent SDK `forkSession` support, exact import/API shape, language parity, persistence model, and concurrency semantics. Transcript says TypeScript-only at teaching time; do not author timeless claim from it (9706-9785, 9910-10205).
- Confirm current Claude Code command syntax for resume, fork, context, compact, clear, rename, rewind; UI labels/shortcuts; web/local interoperability; and whether session IDs can be listed programmatically. Instructor leaves some items uncertain (10255-10305, 10310-10435).
- UNVERIFIED: Verify 22% autocompact buffer, 30-day cleanup/retention statements, zero-cleanup effects, fast-mode behavior, and settings names/legal scopes against versioned official docs (10495-10530, 10770-10795, 10985-11046).
- Transcript distinguishes source provenance but gives no stable canonical finding schema. Need authoritative field contract and handling rules for source URLs, page/section locations, access date, retrieval method, confidence rubric, and citation verification.
- Determine whether source material intends separate lessons for Agent SDK session controls and Claude Code CLI session controls, or one comparison chapter. Keep boundary explicit because interfaces and guarantees differ.
- Determine safe pedagogical framing for destructive actions: clear, rewind, zero cleanup, and artifact deletion. Static page should emphasize outcome, confirmation, backup/verification boundary, and non-guaranteed recovery rather than provide unsafe execution steps.
- UNVERIFIED: Before implementation, reconcile all chapter/objective IDs with `assets/registry.js`, approved archetypes, question-count contract, glossary uniqueness, and verifier constraints; this plan makes no registry or runtime changes.

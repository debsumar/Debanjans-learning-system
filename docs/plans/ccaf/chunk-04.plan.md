## Chunk metadata

- Topic: Claude Certified Architect - Foundations
- Slug: `ccaf`; exam code: `CCA-F` (UNVERIFIED)
- Source: Andrew Brown course transcript, `/tmp/ccaf-chunks/chunk3`
- Source coverage: transcript lines 5524-7364; chunk file lines 1-1841.
- Scope: Agent SDK (UNVERIFIED) coordinator behavior, structured findings, agent definitions, parallel agent calls, observability limits, refactoring for ownership, SDK porting, and built-in agent/task behavior.
- Evidence rule: facts below reflect transcript demonstrations and narration, not independently verified product documentation. SDK names, fields, and parity need release-specific validation.

## Lesson inventory

| # | title | chunk line refs | one-line summary |
|---:|---|---|---|
| 1 | Structured findings and run observability | 5524-5794 | Diagnose empty findings caused by output-format mismatch; improve source attribution and run logs. |
| 2 | Few-shot prompting and scoped research task | 5795-5920 | Run a research task, distinguish needed web access, and consider task-pool use in a larger application. |
| 3 | Agent definition | 5921-6165 | Define sub-agent identity, routing description, system prompt (UNVERIFIED), tools (UNVERIFIED), turns (UNVERIFIED), model (UNVERIFIED), and SDK-specific limits. |
| 4 | Parallel agent calls and permission review | 6166-6548 | Delegate independent research axes concurrently through coordinator instructions; retain least privilege. |
| 5 | Refactoring coordinator code for ownership | 6549-6892 | Split opaque coordinator logic into named units, stateless classes, logs, and human-readable reports. |
| 6 | Porting direct SDK (UNVERIFIED) code to Claude Agent SDK (UNVERIFIED) | 6893-7136 | Compare direct Anthropic SDK (UNVERIFIED) patterns with Agent SDK (UNVERIFIED) query (UNVERIFIED), decorator (UNVERIFIED), and internal MCP-server patterns. |
| 7 | Agent tool architecture | 7137-7222 | Explain agent/task naming, isolated sub-agent context, and hub-and-spoke coordination. |
| 8 | Task-tool follow-along and name migration | 7223-7364 | Build a small researcher/writer coordinator, inspect emitted task messages, and confirm `task` (UNVERIFIED) versus `agent` (UNVERIFIED) naming. |

## Per-lesson plan

### 1. Structured findings and run observability

- Key concepts: coordinator output; structured findings; format mismatch; silent failure; source URL; document name; page number; excerpt attribution; run logs; parallel agents; fallback parsing.
- Exam-relevant facts: a chain agent calling a record-findings operation can fail when it expects a structured finding format but receives a plain list; the transcript describes failures as silent until error logging is added (5524-5685). Source metadata is more useful when it includes URL, document name, and page number; a quoted supporting excerpt is proposed as further improvement (5686-5765). Parallel execution and logging were not the identified cause of the empty findings; a formatting mismatch was (5700-5794).
- Candidate objectives (draft wording + ID `ccaf-cNN-oM`, NN TBD if unknown): `ccaf-cNN-o1` Explain why an agent pipeline can produce an empty result artifact despite completed agent runs. `ccaf-cNN-o2` Identify minimum provenance fields for a research finding. `ccaf-cNN-o3` Select an observability improvement that exposes otherwise silent structured-output failures.
- Glossary terms with g- slugs: `g-structured-output`; `g-format-mismatch`; `g-fallback-parser`; `g-finding-provenance`; `g-source-url`; `g-silent-failure`; `g-run-log`.
- Diagram candidates with archetype from concept|process|comparison|decision|worked-scenario|callout|diagram|mcq-set|active-recall: process - coordinator -> chain agent -> record finding -> result artifact, with mismatch/error-log/fallback branches; worked-scenario - plain list supplied where structured finding is expected; callout - provenance fields versus unsupported finding.
- Comparison/decision table candidates with rows and columns: rows `plain list`, `expected structured finding`, `fallback conversion`, `error log`; columns `input shape`, `recording outcome`, `diagnostic value`, `source attribution`. Rows `URL only`, `URL + document + page`, `URL + document + page + excerpt`; columns `reader traceability`, `evidence precision`, `transcript support`.
- MCQ candidates (count + applied stem ideas): 3. A completed multi-agent run leaves `results.json` (UNVERIFIED) empty; choose first evidence-based debugging target. Select source metadata needed to let a reviewer locate cited evidence. Choose why logging plus parallelism should not be blamed when output-shape evidence identifies mismatch.
- Recall prompts (count + prompts): 4. What input/output mismatch caused findings recording to fail? Which metadata fields improve a finding? Why add error logging before retrying? What does a fallback parser protect against?
- Confusion pairs with discriminator: parallelization versus output-format mismatch - timing/scheduling differs from data-shape incompatibility; log record versus finding record - execution trace differs from durable structured result; source URL versus source evidence - locator differs from locator plus document/page/excerpt context.
- Common wrong turn misconceptions: agents returning text means downstream persistence succeeded; adding parallelism explains every missing output; URL alone establishes sufficiently inspectable evidence.
- Causal step chains only if genuine, else 'none - omission beats invented process': 1. coordinator supplies plain-list output; 2. record-findings expects different structured shape; 3. record operation fails silently; 4. result artifact remains empty; 5. error logging and fallback conversion expose/recover compatible output (5524-5794).

### 2. Few-shot prompting and scoped research task

- Key concepts: few-shot prompting; extracting a measurement from a question; research task; tool visibility; web access; task pool; larger application integration.
- Exam-relevant facts: the transcript introduces few-shot prompting using a measurement-extraction goal and runs `main.py` (UNVERIFIED) to perform research (5795-5845). It distinguishes one agent with web access from another that does not need it, and notes visible progress from three tools (5846-5885). A research task could be incorporated into a larger application for person or LinkedIn-style information gathering, but that use is presented as a possible extension rather than implemented course fact (5886-5920).
- Candidate objectives (draft wording + ID `ccaf-cNN-oM`, NN TBD if unknown): `ccaf-cNN-o1` Distinguish task-required tools from unnecessary tools in a research-agent definition. `ccaf-cNN-o2` Describe a bounded extraction task suitable for few-shot prompting. `ccaf-cNN-o3` Identify what execution progress can, and cannot, prove about research-result quality.
- Glossary terms with g- slugs: `g-few-shot-prompting`; `g-measurement-extraction`; `g-scoped-tool-access`; `g-web-access`; `g-task-pool`; `g-tool-progress`.
- Diagram candidates with archetype from concept|process|comparison|decision|worked-scenario|callout|diagram|mcq-set|active-recall: decision - does task require web access?; process - question -> prompt examples -> research run -> extraction result; callout - progress events show activity, not source quality.
- Comparison/decision table candidates with rows and columns: rows `research agent`, `agent without web need`; columns `task` (UNVERIFIED), `web access`, `observed tool activity`, `least-privilege rationale`. Rows `progress observed`, `useful article returned`; columns `proves execution`, `proves relevance`, `proves accuracy`.
- MCQ candidates (count + applied stem ideas): 2. A measurement-extraction agent has no need for external facts; choose appropriate tool scope. A run shows three tool-progress events but returns a poor article; choose justified conclusion.
- Recall prompts (count + prompts): 3. What was extraction task trying to obtain? Why did one agent have web access while another did not? What does visible tool progress demonstrate?
- Confusion pairs with discriminator: tool progress versus result validation - progress proves calls occurred, validation assesses usefulness/correctness; available tool versus needed tool - availability does not justify permission; few-shot example versus external research - demonstrations guide output while research gathers facts.
- Common wrong turn misconceptions: give every agent web access; treat any returned article as good research; use tool count as a quality metric.
- Causal step chains only if genuine, else 'none - omission beats invented process': 1. define measurement-extraction question; 2. run scoped research task; 3. observe tool-progress events; 4. inspect returned result separately for usefulness (5795-5920).

### 3. Agent definition

- Key concepts: agent definition; blueprint; name; description; prompt/system prompt; allowed tools (UNVERIFIED); disallowed tools (UNVERIFIED); max turns (UNVERIFIED); model (UNVERIFIED); skills; MCP servers; Python/TypeScript SDK (UNVERIFIED) parity; documentation validation.
- Exam-relevant facts: an agent definition is described as a pre-message blueprint defining agent identity, self-knowledge, and allowed actions (5921-5965). Agent name and description influence triggering/routing, and prompt is characterized as the system prompt (5966-6015). UNVERIFIED: The transcript names tools, disallowed tools (UNVERIFIED), max turns (UNVERIFIED), and models as fields, but warns fields may differ by language SDK (6016-6072). Python and TypeScript SDKs are stated not to be one-to-one and may lag differently; generated answers and documentation must be tested rather than trusted without validation (6073-6165).
- Candidate objectives (draft wording + ID `ccaf-cNN-oM`, NN TBD if unknown): `ccaf-cNN-o1` Identify agent-definition fields and their architectural roles. `ccaf-cNN-o2` Explain why name and description require deliberate routing language. `ccaf-cNN-o3` Assess an SDK-field claim by validating it against the target SDK/version. `ccaf-cNN-o4` Apply allow/disallow tool controls to an agent boundary.
- Glossary terms with g- slugs: `g-agent-definition`; `g-agent-name`; `g-agent-description`; `g-system-prompt`; `g-allowed-tools`; `g-disallowed-tools`; `g-max-turns`; `g-model-selection`; `g-sdk-parity`; `g-mcp-server`; `g-skills`.
- Diagram candidates with archetype from concept|process|comparison|decision|worked-scenario|callout|diagram|mcq-set|active-recall: concept - agent definition with identity, routing, instructions, controls, and model (UNVERIFIED); comparison - Python versus TypeScript field availability; decision - documented/generated field claim -> verify target SDK -> use or reject.
- Comparison/decision table candidates with rows and columns: rows `name`, `description`, `prompt`, `tools`, `disallowed tools (UNVERIFIED)`, `max turns (UNVERIFIED)`, `model (UNVERIFIED)`; columns `definition role`, `transcript claim`, `validation needed`. Rows `Python SDK (UNVERIFIED)`, `TypeScript SDK (UNVERIFIED)`; columns `field availability`, `release parity`, `implementation check`.
- MCQ candidates (count + applied stem ideas): 4. Agent fails to be selected for an expected task; choose field most directly implicated. Documentation shows an option unavailable in Python; choose next action. Pick control preventing an otherwise allowed tool. Choose definition element that establishes system-level behavior rather than user request content.
- Recall prompts (count + prompts): 5. Define agent definition. Which fields affect routing? What does the transcript call `prompt`? Why cannot TypeScript documentation alone establish Python support? What must be tested before depending on generated SDK code?
- Confusion pairs with discriminator: name versus description - identity label versus routing/trigger explanation; prompt versus user message - stable system instruction versus request-time content; tools versus disallowed tools (UNVERIFIED) - granted set versus explicit prohibition; SDK documentation versus tested capability - claim versus validated target behavior.
- Common wrong turn misconceptions: all SDKs expose identical fields; generated code is authoritative; a generic agent name/description has no routing effect; `prompt` here is only a user prompt.
- Causal step chains only if genuine, else 'none - omission beats invented process': 1. choose target SDK and version; 2. identify required agent-definition behavior; 3. verify exact supported fields in target implementation; 4. configure and test rather than infer cross-SDK parity (5921-6165).

### 4. Parallel agent calls and permission review

- Key concepts: parallel tool calls; coordinator; independent research axes; one response; scheduling; agent loop; internal-loop visibility; bypass permissions; least privilege; source attribution.
- Exam-relevant facts: a coordinator can be instructed to invoke multiple research-agent calls simultaneously for independent axes instead of waiting between calls (6166-6300). Parallelization is driven by coordinator instructions such as running all axes in one turn and not waiting, rather than an automatically proven implementation detail (6301-6380). Each agent can run its own loop; transcript says internal loop activity may not be visible through the SDK (6381-6450). `bypass permissions` (UNVERIFIED) is removed in the demonstration because permission scope must be considered, even if contemplated action is web search (6451-6548).
- Candidate objectives (draft wording + ID `ccaf-cNN-oM`, NN TBD if unknown): `ccaf-cNN-o1` Identify independent work suitable for concurrent agent delegation. `ccaf-cNN-o2` Draft coordinator instructions that request simultaneous agent calls. `ccaf-cNN-o3` Distinguish successful final output from proof that internal loops were visible or parallel. `ccaf-cNN-o4` Apply least privilege when reviewing an agent permission mode.
- Glossary terms with g- slugs: `g-parallel-agent-calls`; `g-coordinator`; `g-independent-axis`; `g-agent-loop`; `g-internal-observability`; `g-scheduling`; `g-bypass-permissions`; `g-least-privilege`.
- Diagram candidates with archetype from concept|process|comparison|decision|worked-scenario|callout|diagram|mcq-set|active-recall: process - coordinator fans out five independent research axes and gathers findings; comparison - sequential delegation versus instructed simultaneous delegation; decision - task independence and permissions before parallelization; callout - final result is not visibility into internal loop.
- Comparison/decision table candidates with rows and columns: rows `sequential calls`, `parallel calls`; columns `dependency requirement`, `coordinator wording`, `wait behavior`, `observability limitation`. Rows `allow scoped web search`, `bypass permissions` (UNVERIFIED); columns `scope`, `review concern`, `transcript decision`.
- MCQ candidates (count + applied stem ideas): 4. Five source axes have no data dependency; choose coordinator approach. A parent sees only completion; choose what cannot be concluded about sub-agent internals. A generated configuration adds bypass mode; choose secure review response. Select work that must remain sequential because second task needs first output.
- Recall prompts (count + prompts): 4. What makes tasks eligible for parallel delegation? What coordinator wording signals simultaneous calls? Why is final output insufficient evidence of internal progress? Why was bypass permission removed?
- Confusion pairs with discriminator: parallel agents versus parallel tool calls - transcript uses both around coordinator-delegated agent work; verify exact SDK semantics; concurrent scheduling versus internal-loop streaming - concurrent work does not provide per-step visibility; permission bypass versus bounded web access - broad override differs from task-specific capability.
- Common wrong turn misconceptions: agents automatically run in parallel once defined; completion proves no looping; bypass permissions is harmless whenever tools only research; any multi-step task is parallel-safe.
- Causal step chains only if genuine, else 'none - omission beats invented process': 1. identify independent axes; 2. state simultaneous delegation/no waiting in coordinator instruction; 3. execute delegated agents; 4. gather and deduplicate/rank findings; 5. inspect permission scope and output because internal loops may remain opaque (6166-6548).

### 5. Refactoring coordinator code for ownership

- Key concepts: coordinator refactor; functions as documentation; stateless class; static methods; domain ownership; partition logic; inputs/outputs; mockability; coverage report; JSONL logging proposal; human-readable report; technical ownership.
- Exam-relevant facts: transcript advocates decomposing a large coordinator routine so named functions make flow readable and testable (6549-6680). Stateless classes with static functions are preferred in the demonstration to group operations by domain rather than leave loose functions (6681-6760). Smaller functions with clear inputs/outputs improve mocking and test writing (6761-6810). Coordinator code is moved to its own library file, then run to confirm it still works; logs and a timestamped, human-readable coverage report are added/checked (6811-6892). JSON-structured logging is suggested as a future improvement, not presented as completed behavior (6860-6892).
- Candidate objectives (draft wording + ID `ccaf-cNN-oM`, NN TBD if unknown): `ccaf-cNN-o1` Identify coordinator-code signals that justify decomposition. `ccaf-cNN-o2` Explain how explicit inputs/outputs improve agent-system tests. `ccaf-cNN-o3` Distinguish implemented reporting from a proposed logging enhancement. `ccaf-cNN-o4` Validate a refactor by running its preserved workflow and checking produced artifacts.
- Glossary terms with g- slugs: `g-coordinator-refactor`; `g-stateless-class`; `g-static-method`; `g-domain-ownership`; `g-mockability`; `g-coverage-report`; `g-jsonl-logging`; `g-technical-ownership`; `g-human-readable-report`.
- Diagram candidates with archetype from concept|process|comparison|decision|worked-scenario|callout|diagram|mcq-set|active-recall: process - large coordinator loop -> named domain functions/classes -> run -> logs/report validation; comparison - monolithic conditional flow versus named operations; callout - proposed JSON logs are not confirmed implementation.
- Comparison/decision table candidates with rows and columns: rows `large conditional block`, `named function`, `stateless domain class`; columns `ownership`, `readability`, `test seam`, `mocking ease`. Rows `coordinator log`, `timestamped coverage report`, `JSONL proposal`; columns `artifact type`, `status in transcript`, `consumer`.
- MCQ candidates (count + applied stem ideas): 3. A coordinator function contains many opaque conditional branches; choose refactoring goal. A unit test cannot isolate partition behavior; choose design improvement. A report appears in logs but is hard to consume; choose demonstrated versus proposed artifact action.
- Recall prompts (count + prompts): 4. Why can functions act as documentation? What does a stateless class group? How do simpler inputs/outputs help mocks? Which report characteristics were requested?
- Confusion pairs with discriminator: refactor versus behavior change - reorganizing code retains observed workflow; logging versus reporting - event stream differs from durable human-readable artifact; loose utility versus domain class - no owner versus explicit ownership group; implemented capability versus preferred future improvement - run-confirmed report versus suggested JSONL format.
- Common wrong turn misconceptions: extraction into any function automatically improves design; a successful refactor needs no runtime validation; subjective code preference proves SDK behavior; log output substitutes for a reusable report.
- Causal step chains only if genuine, else 'none - omission beats invented process': 1. identify oversized coordinator flow and domain-specific blocks; 2. extract named operations and group domain operations; 3. move coordinator ownership to library boundary; 4. run workflow; 5. confirm logs and timestamped report artifact (6549-6892).

### 6. Porting direct SDK (UNVERIFIED) code to Claude Agent SDK (UNVERIFIED)

- Key concepts: direct Anthropic SDK (UNVERIFIED); Claude Agent SDK (UNVERIFIED); porting; async client; query (UNVERIFIED); decorators (UNVERIFIED); MCP server (UNVERIFIED); tool JSON; internal MCP server (UNVERIFIED); requirements; smoke test.
- Exam-relevant facts: transcript ports a coordinator application from direct Anthropic SDK (UNVERIFIED) use to Agent SDK (UNVERIFIED) to explore Agent SDK (UNVERIFIED) arguments in a continuing example (6893-6980). In a smaller comparison, direct tool handling is replaced by decorator-based (UNVERIFIED) functions and an SDK MCP server (UNVERIFIED) passed with tools (6981-7055). The transcript characterizes this as an internal MCP server (UNVERIFIED) and notes the Agent SDK (UNVERIFIED) tool-use code is smaller; exact implementation must still be tested (7056-7095). Tool definitions can be moved out of coordinator code via a factory/closure pattern in the demonstrated refactor; the port is run and reported as completing without issue (7096-7136).
- Candidate objectives (draft wording + ID `ccaf-cNN-oM`, NN TBD if unknown): `ccaf-cNN-o1` Compare direct SDK (UNVERIFIED) tool handling with the Agent SDK (UNVERIFIED) pattern demonstrated. `ccaf-cNN-o2` Identify why a small porting example can clarify framework differences. `ccaf-cNN-o3` Validate a port after dependency and tool-definition changes. `ccaf-cNN-o4` Explain the transcript's internal-MCP-server characterization without generalizing beyond tested SDK behavior.
- Glossary terms with g- slugs: `g-direct-sdk`; `g-agent-sdk`; `g-sdk-port`; `g-tool-decorator`; `g-sdk-mcp-server`; `g-tool-json`; `g-tool-factory`; `g-closure`; `g-async-client`; `g-smoke-test`.
- Diagram candidates with archetype from concept|process|comparison|decision|worked-scenario|callout|diagram|mcq-set|active-recall: comparison - direct SDK (UNVERIFIED) tool handling versus Agent SDK (UNVERIFIED) decorator (UNVERIFIED)/MCP-server path; process - copy baseline -> port dependencies/coordinator -> inspect tool boundary -> execute smoke test; callout - internal MCP description is transcript interpretation, verify target version.
- Comparison/decision table candidates with rows and columns: rows `direct SDK (UNVERIFIED) example`, `small Agent SDK (UNVERIFIED) port`, `larger Agent SDK (UNVERIFIED) port`; columns `tool declaration`, `client/query (UNVERIFIED) path`, `MCP server (UNVERIFIED) use`, `validation`. Rows `tool JSON file`, `decorated tool function`, `tool factory`; columns `location`, `coupling`, `state handling`.
- MCQ candidates (count + applied stem ideas): 3. A team wants to isolate porting differences; choose initial example size. Identify Agent SDK (UNVERIFIED) component used to provide decorated tools in transcript. After converting tool handling, choose required validation before claiming success.
- Recall prompts (count + prompts): 4. Why port a continuing coordinator example? What changed in tool handling in small port? What did transcript call internal MCP? Why run after tool-definition refactor?
- Confusion pairs with discriminator: direct SDK (UNVERIFIED) versus Agent SDK (UNVERIFIED) - lower-level direct client pattern versus Agent SDK (UNVERIFIED) query (UNVERIFIED)/decorator (UNVERIFIED) pattern; MCP server (UNVERIFIED) versus remote MCP claim - transcript calls it internal; do not infer remote deployment; decorator (UNVERIFIED) location versus tool ownership - syntax placement differs from domain ownership; port completion versus feature equivalence - successful run verifies observed scenario only.
- Common wrong turn misconceptions: requirements automatically update during a port; fewer lines establish correct behavior; a tool JSON file must remain after decorator (UNVERIFIED) migration; any MCP server (UNVERIFIED) here is network-hosted.
- Causal step chains only if genuine, else 'none - omission beats invented process': 1. choose direct-SDK baseline; 2. port imports/client/coordinator to Agent SDK (UNVERIFIED); 3. rework tool boundary with decorators (UNVERIFIED) and SDK MCP server (UNVERIFIED); 4. review coupling/location; 5. execute application and inspect successful run (6893-7136).

### 7. Agent tool architecture

- Key concepts: agent tool; former task tool name; sub-agent; isolated context; system prompt; tool set; blocking parent; scheduling; coordinator; hub-and-spoke; explicit context handoff.
- Exam-relevant facts: the agent tool is described as capability to spin up an agent and was previously called task tool; older naming may still appear in code/docs (7137-7172). A spawned sub-agent has isolated context, its own system prompt, and its own tools (7173-7198). Spawning does not guarantee parallel execution; transcript says parent generally blocks until a single sub-agent completes and reports back, depending on scheduling/prompting (7199-7212). In the hub-and-spoke analogy, spokes do not share parent context, so required knowledge must be supplied explicitly (7213-7222).
- Candidate objectives (draft wording + ID `ccaf-cNN-oM`, NN TBD if unknown): `ccaf-cNN-o1` Describe context and tool isolation for a spawned sub-agent. `ccaf-cNN-o2` Distinguish agent spawning from guaranteed parallel scheduling. `ccaf-cNN-o3` Identify information that must be passed explicitly from coordinator to sub-agent. `ccaf-cNN-o4` Recognize `task` (UNVERIFIED) and `agent` (UNVERIFIED) as naming migration evidence requiring version validation.
- Glossary terms with g- slugs: `g-agent-tool`; `g-task-tool`; `g-sub-agent`; `g-isolated-context`; `g-context-handoff`; `g-hub-and-spoke`; `g-parent-blocking`; `g-agent-scheduling`.
- Diagram candidates with archetype from concept|process|comparison|decision|worked-scenario|callout|diagram|mcq-set|active-recall: concept - hub coordinator with isolated spoke agents; comparison - shared assumed context versus explicit passed context; decision - need parallel output? determine scheduling/prompting, do not infer from spawn; callout - `task` (UNVERIFIED) historical name versus current `agent` (UNVERIFIED) label.
- Comparison/decision table candidates with rows and columns: rows `coordinator`, `sub-agent`; columns `context`, `system prompt`, `tools`, `knowledge supplied`. Rows `spawned agent`, `parallel work`; columns `guaranteed?`, `dependency`, `observed behavior`.
- MCQ candidates (count + applied stem ideas): 3. A researcher sub-agent lacks a parent-provided constraint; choose cause. A design needs concurrently completed research; choose why spawn alone is insufficient. An old tutorial refers to task tool; choose validation action.
- Recall prompts (count + prompts): 4. What does sub-agent isolation include? Does spawning imply parallelism? What must coordinator pass to a sub-agent? What architecture analogy is used?
- Confusion pairs with discriminator: agent spawn versus parallel execution - creation differs from scheduling; isolated context versus copied parent context - no inherited knowledge unless supplied; task tool versus agent tool - historical/current naming, validate exact version; coordinator vs sub-agent - orchestrates/receives versus executes bounded delegated work.
- Common wrong turn misconceptions: a sub-agent sees all parent conversation; each sub-agent runs concurrently by default; renamed tool names prove exact current API contract.
- Causal step chains only if genuine, else 'none - omission beats invented process': 1. coordinator determines delegated need; 2. passes task, required context, system prompt, and tools; 3. agent runs in isolated context; 4. parent receives report, potentially after blocking for completion (7137-7222).

### 8. Task-tool follow-along and name migration

- Key concepts: simple task-tool demo; researcher agent; writer agent; coordinator prompt; topic; query (UNVERIFIED); allowed tools (UNVERIFIED); agent definitions; message types; task start/progress/notification; permission mode; historical naming discrepancy.
- Exam-relevant facts: the follow-along requests a simple built-in task-tool demonstration using existing code, then reduces it to researcher gathers three facts and writer turns facts into short paragraphs (7223-7278). The coordinator uses a system prompt, model (UNVERIFIED), allowed `agent` (UNVERIFIED) tool, and researcher/writer definitions; generated UNVERIFIED: bypass-permissions mode is removed during review (7279-7318). The transcript inspects task-start, task-progress, and task-notification message types (7319-7335). It concludes that references to task and observed allowed `agent` (UNVERIFIED) behavior point to a naming change: task is agent/agent is task in this transcript, while emphasizing inability to find clear confirming documentation (7336-7364).
- Candidate objectives (draft wording + ID `ccaf-cNN-oM`, NN TBD if unknown): `ccaf-cNN-o1` Map a researcher/writer delegation into coordinator responsibilities. `ccaf-cNN-o2` Identify message categories useful for task lifecycle observation. `ccaf-cNN-o3` Review generated permission mode before executing an agent example. `ccaf-cNN-o4` Handle a task/agent terminology mismatch through runtime and version-specific validation.
- Glossary terms with g- slugs: `g-researcher-agent`; `g-writer-agent`; `g-coordinator-prompt`; `g-task-start-message`; `g-task-progress-message`; `g-task-notification-message`; `g-permission-mode`; `g-terminology-migration`.
- Diagram candidates with archetype from concept|process|comparison|decision|worked-scenario|callout|diagram|mcq-set|active-recall: worked-scenario - topic -> coordinator -> researcher (three facts) -> writer (short paragraphs) -> short answer; process - task lifecycle messages; decision - docs/name mismatch -> inspect allowed tool/runtime behavior -> record version caveat; callout - remove bypass mode before execution review.
- Comparison/decision table candidates with rows and columns: rows `researcher`, `writer`, `coordinator`; columns `input`, `responsibility`, `output`, `tool/context boundary`. Rows `task start`, `task progress`, `task notification`; columns `lifecycle point`, `what observer learns`, `what it does not prove`. Rows `task` (UNVERIFIED), `agent` (UNVERIFIED); columns `transcript evidence`, `documentation clarity`, `required verification`.
- MCQ candidates (count + applied stem ideas): 4. A short-article coordinator needs factual collection and prose generation; assign roles. Select message type for in-flight visibility. A generated example enables bypass mode; choose action. Documentation and emitted identifiers disagree on task/agent; choose defensible conclusion.
- Recall prompts (count + prompts): 5. What was researcher asked to return? What was writer asked to create? Which message categories were inspected? What permission mode was removed? Why is task/agent equivalence a version-validation issue?
- Confusion pairs with discriminator: researcher versus writer - fact collection versus transformation into prose; task-progress message versus final result - lifecycle signal versus completed content; historical task name versus current agent identifier - terminology evidence versus verified API guarantee; simple demo versus production design - illustration versus sufficient operational controls.
- Common wrong turn misconceptions: researcher and writer share context automatically; generated bypass mode needs no review; message events prove source quality; task/agent wording can be normalized without recording version ambiguity.
- Causal step chains only if genuine, else 'none - omission beats invented process': 1. coordinator receives topic; 2. delegates fact collection to researcher; 3. receives research facts; 4. delegates prose transformation to writer; 5. returns short answer while lifecycle messages can be inspected (7223-7364).

## Proposed chapter mapping

| Proposed chapter | Lessons | Draft chapter purpose | Objective IDs | Source refs |
|---|---:|---|---|---|
| TBD: Agent pipeline reliability and evidence | 1-2 | Design inspectable structured-output and research flows with bounded tool access. | `ccaf-cNN-o1` through `ccaf-cNN-o3` per lessons 1-2 | 5524-5920 |
| TBD: Agent definition and orchestration | 3-4 | Configure agent boundaries, verify SDK reality, and coordinate independent work safely. | `ccaf-cNN-o1` through `ccaf-cNN-o4` per lessons 3-4 | 5921-6548 |
| TBD: Agent implementation ownership | 5-6 | Refactor and port coordinator systems while retaining testability and validation. | `ccaf-cNN-o1` through `ccaf-cNN-o4` per lessons 5-6 | 6549-7136 |
| TBD: Delegation primitives and lifecycle | 7-8 | Use isolated sub-agents, explicit context handoff, task lifecycle signals, and terminology validation. | `ccaf-cNN-o1` through `ccaf-cNN-o4` per lessons 7-8 | 7137-7364 |

- Chapter numbers remain `NN` until full 10-chunk synthesis establishes course order and official CCA-F (UNVERIFIED) objective mapping.
- Do not treat transcript headings as final exam-domain names. Preserve source claims, then map against official exam guide during registry-first planning.

## Hands-on / lab content

| Lab candidate | Transcript basis | Learner artifact | Guardrail / validation |
|---|---|---|---|
| Diagnose empty findings | Structured findings fail from list/structured format mismatch (5524-5794). | Failure analysis: expected schema, actual shape, error-log evidence, fallback decision. | Use mocked/static artifacts only; verify error surfaced and fallback output is traceable. |
| Scope research tools | One research task has web access while another does not need it (5795-5920). | Tool-access matrix for two agents. | Grant only task-required capability; do not use real remote assets in shipped learning pages. |
| Validate agent-definition fields | Python and TypeScript feature parity can differ (5921-6165). | SDK/version validation checklist. | Record target language/version/evidence; do not copy unverified generated fields. |
| Plan independent parallel delegation | Coordinator requests concurrent independent research axes (6166-6548). | Dependency map marking parallel-safe versus sequential work. | Explain no dependencies before fan-out; keep permission review explicit. |
| Refactor coordinator on paper | Named functions/classes improve ownership and test seams (6549-6892). | Before/after responsibility map plus test-seam list. | Preserve behavior in design; check produced logs/report after any implementation. |
| Compare porting patterns | Direct SDK and Agent SDK (UNVERIFIED) tool patterns differ (6893-7136). | Side-by-side pattern inventory, not executable course code. | Confirm target-version API and run smoke test before asserting equivalence. |
| Trace researcher/writer delegation | Researcher returns facts; writer creates paragraphs; messages expose lifecycle (7223-7364). | Sequence trace with explicit context inputs and lifecycle events. | Keep agent context boundaries visible; do not assume shared memory or bypass permissions. |

- This plan creates no runnable lab assets. Future implementation must preserve offline-first constraints: no build, npm, remote assets, modules, or runtime registry dependency; pages remain JS-disabled readable; shared assets retain presentation ownership; run `tools/verify.ps1` after implementation changes.

## Open questions and gaps

- Official CCA-F (UNVERIFIED) domain/objective list is absent from this chunk. Confirm all `ccaf-cNN-oM` IDs and chapter numbers during registry-first synthesis.
- Transcript contains commentary about documentation quality and observed SDK behavior. Determine which claims are assessment-relevant versus historical/version-specific before writing learner-facing facts.
- Exact SDK version, Agent SDK (UNVERIFIED) language bindings, and current terminology for `task` (UNVERIFIED)/`agent` (UNVERIFIED) are not reliably established here. Verify against target-version primary documentation and runnable examples.
- Exact `AgentDefinition (UNVERIFIED)` field availability is explicitly uncertain across Python and TypeScript (5921-6165). Do not hard-code field lists from this chunk alone.
- Transcript calls Agent SDK (UNVERIFIED) tool setup an internal MCP server (UNVERIFIED) (6981-7095). Verify whether this is formal product terminology and what architecture claims may safely appear in study material.
- Internal sub-agent loop visibility is described as unavailable/limited in this example (6381-6450). Confirm whether current SDK observability APIs alter this constraint.
- Parallelism is requested through coordinator prompting in demonstration (6166-6548), but actual scheduling guarantees, limits, and billing/usage telemetry are not established.
- Findings schema, fallback parser behavior, JSON report schema, and report retention rules are not specified. Do not invent contracts.
- Few-shot prompting opening is brief and lacks complete prompt examples (5795-5920). Pull adjacent chunk evidence before making a standalone detailed lesson.
- Hands-on narration uses Python files, browser login, web access, and external research. Static topic implementation must describe concepts without adding network/runtime dependencies.
